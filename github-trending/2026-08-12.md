---
title: "GitHub Trending 2026-08-12"
tags: [github-trending]
---

오늘 트렌딩 상위권은 최근 며칠 이어진 얼굴들이 순위만 바꿔 다시 자리를 지킨 하루입니다. Claude Code용 다이어그램 스킬 diagram-design이 이틀 연속 1위를 지켰고, 230개가 넘는 에이전트 페르소나 모음 agency-agents는 나흘째, 지식 그래프 기반 컨텍스트 인프라 semantica는 사흘째 트렌딩에 올랐습니다. 여기에 여러 코딩 에이전트를 워크트리 단위로 나란히 돌리는 오케스트레이션 데스크톱 앱 orca가 4만 스타를 넘긴 채 새로 진입했고, 이메일·문서·태스크·에이전트를 하나의 그래프로 묶은 올인원 워크스페이스 macro가 합류하면서, "에이전트를 어떻게 조직하고 신뢰할 것인가"라는 질문이 도구 하나에 그치지 않고 워크스페이스 전체의 설계 문제로 번지고 있다는 인상을 줍니다.

## cathrynlavery/diagram-design

- <https://github.com/cathrynlavery/diagram-design>
- 언어: HTML
- 오늘 스타: 2,951 (총 8,809)

Claude Code 등 에이전트가 만드는 다이어그램이 획일적이라는 문제의식에서 출발해, 외부 이미지나 빌드 단계 없이 그 자체로 완결된 HTML+SVG 파일로 다이어그램을 만들어내는 스킬입니다. minimal light·minimal dark·full editorial 세 가지 시각 변형으로 27가지 다이어그램 타입을 지원하며, `SKILL.md` 인덱스가 37개 참조 파일 중 필요한 것만 그때그때 불러오는 점진적 공개 구조로 에이전트의 컨텍스트를 아낍니다. 웹사이트를 60초 만에 스캔해 색상·타이포그래피를 뽑아내는 브랜드 온보딩과 draw.io·Mermaid 다이어그램을 가져와 다시 그리는 기능도 갖췄고, 최근 버전 2.0에서는 공유 메모리 허브와 write-back 경로를 표현하는 "Loop" 다이어그램 타입이 추가됐습니다. 절차뿐 아니라 시각적 취향까지 스킬 파일로 표준화하려는 흐름이 이틀 연속 최상위권을 지키고 있다는 점이 눈에 띕니다.

## msitarzewski/agency-agents

- <https://github.com/msitarzewski/agency-agents>
- 언어: Shell
- 오늘 스타: 1,969 (총 144,348)

엔지니어링·디자인·마케팅·헬스케어 등 여러 분야에 걸쳐 230개가 넘는 전문 에이전트 페르소나를 모아둔 저장소로, 나흘째 트렌딩 상위권을 지켰습니다. 각 에이전트가 정체성·워크플로·성공 지표·코드 예시를 갖췄고, 변환 스크립트로 Claude Code·GitHub Copilot·Cursor·Aider·Windsurf 등 여러 도구의 네이티브 포맷으로 설치할 수 있으며, 로스터 탐색과 원클릭 설치가 가능한 macOS·Linux·Windows 네이티브 데스크톱 앱도 갖췄습니다. Game Development(Unity·Unreal·Godot·Roblox), 학술 연구, GIS, 헬스케어 등으로 분야를 계속 넓히고 있고 중국어·포르투갈어·러시아어·한국어 등 다국어 번역까지 유지되는 규모를 보면, "역할별 에이전트 페르소나를 표준 포맷으로 유통한다"는 접근이 스킬 파일 생태계의 기본 인프라처럼 자리잡고 있다는 신호로 읽힙니다.

## stablyai/orca

- <https://github.com/stablyai/orca>
- 언어: TypeScript
- 오늘 스타: 1,215 (총 43,538)

Codex, Claude Code, OpenCode 같은 코딩 에이전트 여러 개를 각자의 git worktree에서 동시에 돌리고 한 화면에서 관리하는 Electron 기반 데스크톱 앱입니다. WebGL로 렌더링되는 커스텀 터미널이 무한 분할과 영속적인 스크롤백을 지원하고, 내장 크로미움 창의 "디자인 모드"로 UI 요소를 클릭하면 HTML·CSS·스크린샷을 그대로 에이전트 프롬프트에 넣을 수 있으며, SSH 원격 실행과 iOS·Android 동반 앱으로 원격 모니터링까지 갖췄습니다. 하나의 프롬프트를 여러 에이전트에 동시에 던져 결과를 비교·병합하는 구조와, 에이전트가 만든 diff에 직접 댓글을 달아 피드백을 되돌려주는 기능을 보면, "에이전트 여러 개를 병렬로 부리는 워크플로"가 CLI 도구를 넘어 전용 데스크톱 환경으로 옮겨가는 흐름을 보여줍니다.

## semantica-agi/semantica

- <https://github.com/semantica-agi/semantica>
- 언어: Python
- 오늘 스타: 834 (총 5,503)

LLM과 에이전트 프레임워크 아래에 놓이는 그래프 네이티브 컨텍스트 인프라로, 임베딩 유사도 대신 지식 그래프에 근거와 출처를 저장하는 것을 목표로 사흘째 트렌딩에 올랐습니다. Ingest→Parse→Extract→Conflict Detection→Dedup→Knowledge Graph로 이어지는 파이프라인 위에서 RDF(Oxigraph, Blazegraph)와 LPG(Neo4j 등) 저장소를 함께 지원하고, `record_decision()`·`trace_decision_chain()`으로 AI의 판단 근거를 영구적인 그래프 노드로 남기는 것이 핵심입니다. 총 스타가 사흘 만에 3,800개대에서 5,500개대로 늘어난 것을 보면, 금융·의료·정부처럼 설명 가능성이 중요한 규제 산업을 겨냥한 이 접근에 대한 관심이 하루짜리 화제로 끝나지 않고 있다는 것을 알 수 있습니다.

## macro-inc/macro

- <https://github.com/macro-inc/macro>
- 언어: Rust
- 오늘 스타: 248 (총 1,355)

이메일·채팅·문서·태스크·AI 에이전트·CRM을 하나의 인터페이스로 묶고, 이 모든 것을 양방향 그래프 데이터베이스로 서로 링크하는 올인원 워크스페이스입니다. SolidJS 기반 프런트엔드가 웹·Tauri 데스크톱·모바일을 아우르고, 백엔드는 42개 서비스와 167개 라이브러리로 이뤄진 Rust 마이크로서비스가 헥사고날 아키텍처로 도메인 코어와 어댑터를 분리하며, Cloudflare Durable Objects 기반 CRDT로 실시간 공동 편집을 지원합니다. 매일 밤 대화·이메일·태스크 활동을 종합해 에이전트와 사용자에게 넘겨주는 "팀 메모리" 시스템과 MCP 기반 에이전트 통합이 특징으로, AGPLv3로 완전히 오픈소스이면서 SOC 2 Type II 인증까지 받은 점에서 "이슈 트래커가 실제 대화가 오가는 곳과 분리되면 정확성을 잃는다"는 문제의식을 어디까지 인프라로 풀어냈는지 살펴볼 만합니다.

## 오늘의 히든잼

### Vinalinux-Org/Nothan-OS

- <https://github.com/Vinalinux-Org/Nothan-OS>
- 언어: C
- 스타: 110 (2026-01-22 생성)

BeagleBone Black(TI AM3358, ARMv7-A)을 대상으로 부트로더·커널·libc·컴파일러·유저스페이스까지 전부 밑바닥부터 직접 짜는 베어메탈 ARM 운영체제입니다. 드라이버·파일시스템·스케줄러·메모리 관리가 커널 주소 공간(`0xC0000000`)을 공유하는 모놀리식 커널 구조를 채택했고, `fork()+exec()` 대신 `spawn()` 시스템콜을 쓰는 등 Unix 관행을 의도적으로 벗어난 설계도 눈에 띕니다. FAT32·devfs·procfs를 지원하는 VFS, 슬랩 캐싱을 갖춘 버디 할당자, ARMv7 바이너리를 뽑는 자체 파이썬 기반 컴파일러(NothCC)까지 갖췄고, 테스트는 QEMU 없이 실제 BeagleBone Black 하드웨어에서만 진행하며 UART 시리얼 로그로 디버깅합니다. 모든 소스 파일에 저작자 헤더를 요구하고 "코드를 베끼지 말고 패턴을 이해한 뒤 독립적으로 재구현하라"는 기여 가이드를 두는 등, "100% 손으로 직접 짠다"는 원칙을 조직 문화 차원에서 강제하는 프로젝트입니다.

### ericdrowell/brometal

- <https://github.com/ericdrowell/brometal>
- 언어: TypeScript
- 스타: 98 (2026-07-21 생성)

TypeScript로 셰이더(`.shader.ts`)를 작성하면 파서·타입체커·시맨틱 분석기·옵티마이저를 거쳐 WGSL과 타입이 붙은 메타데이터(`.shader.gen.ts`)로 컴파일해주는 WebGPU 셰이더 컴파일러이자 런타임입니다. 어트리뷰트 위치, 버퍼 레이아웃, 유니폼 오프셋을 전부 빌드 타임에 확정해두기 때문에 브라우저에서는 탐색 과정 없이 미리 계산된 실행 계획만 돌리면 되고, 그 결과 런타임이 gzip 기준 약 7KB로 작습니다. WebGL2를 버리고 WebGPU만 지원해 컴퓨트 셰이더와 스토리지 버퍼를 확보했으며, vitest 기반 유닛 테스트로 컴파일된 WGSL 텍스트를 검증하고 실제 브라우저를 띄워 픽셀 출력을 비교하는 GPU 테스트까지 별도로 갖췄습니다. 컴파일 타임에 파일:줄:칸 단위로 오류를 잡아내고 셰이더 스테이지 간 varying 계약을 강제하는 설계는, 타입 안전한 GPU 프로그래밍 도구를 처음부터 설계할 때 참고할 만합니다.

### harshmandan/svelte-check-native

- <https://github.com/harshmandan/svelte-check-native>
- 언어: Rust
- 스타: 93 (2026-04-16 생성)

JavaScript로 짜인 `svelte-check`를 그대로 대체하는, 동일한 플래그와 진단 결과를 내면서 훨씬 빠른 Rust 타입 체커입니다. `.svelte` 파일을 Svelte 5 AST로 바꾸는 parser, 룬(runes)·스코프·바인딩을 분석하는 analyze, 타입체킹용 `.svelte.ts` 오버레이를 생성하는 emit, 컴파일러 경고를 네이티브로 옮긴 svn-lint, TypeScript 설정과 `tsbuildinfo` 캐싱을 관리하는 typecheck 등 역할별 크레이트로 나뉜 구조입니다. TypeScript 7의 네이티브 `tsc` 바이너리를 그대로 활용하면서 증분 컴파일 캐시를 유지해, 1,359개 파일짜리 SvelteKit 모노레포에서 콜드 스타트를 40초에서 2.5초로, 웜 런은 0.2초(40배)까지 줄였다고 공개했습니다. `svelte2tsx`의 `.v5` 픽스처 코퍼스를 호환성 검증 게이트로 쓰면서도 CSS 린팅이나 watch 모드는 의도적으로 빼둔 점에서, 기존 도구를 다른 언어로 다시 짤 때 어디까지 기능 범위를 맞추고 어디서 잘라낼지 판단한 흔적을 볼 수 있습니다.

### VoolFI71/go-arena

- <https://github.com/VoolFI71/go-arena>
- 언어: Go
- 스타: 8 (2026-02-01 생성)

Go의 가비지 컬렉터에 의존하지 않고 메모리 청크의 연결 리스트(`[][]byte`)와 커서 기반 할당으로 동작하는 범프 포인터 아레나 할당자로, C10M급 서버나 고빈도 트레이딩처럼 지연시간이 민감한 워크로드를 겨냥합니다. 청크가 가득 차면 새 청크를 자동으로 늘리고, `Reset()`은 메모리를 실제로 해제하는 대신 인덱스만 되돌려 O(1)에 전체를 비우며, 동시성 환경을 위한 스레드 세이프 `ArenaPool` 오브젝트 재사용 래퍼도 갖췄습니다. 100만 회 스트레스 테스트를 포함한 벤치마크 스위트에서 구조체 할당은 약 4.7배, 100만 길이 슬라이스 생성은 O(1) 포인터 범핑 덕분에 약 60만 배까지 빨라졌다고 밝히고 있고, API 변경을 잡아내는 `api_contract_test.go`와 잘못 쓰는 패턴을 정리한 안티패턴 문서까지 갖췄습니다. 스타는 아직 한 자릿수대지만, GC 우회 아레나 할당자를 프로덕션 안전장치(SemVer 정책, 데이터 유출 방지 논의)까지 갖춰 설계하는 법을 볼 수 있는 자료입니다.

### sema-lisp/sema

- <https://github.com/sema-lisp/sema>
- 언어: Rust
- 스타: 31 (2026-02-11 생성)

LLM 에이전트를 SDK가 아니라 언어의 1급 원시 타입으로 다루는 Scheme 계열 Lisp로, 재시도·캐싱·비용 추적·툴 디스패치 같은 에이전트 스캐폴딩을 런타임 안에 직접 내장했습니다. 멀티턴 대화를 불변 데이터 구조로 다루고, 구조화된 데이터 추출·분류·스트리밍·비전·비용 추적을 언어 차원에서 지원하며, NaN-boxing으로 값을 표현하는 바이트코드 VM 위에서 Anthropic·OpenAI·Gemini·Ollama·Groq 등 여러 LLM 프로바이더를 트레이트 하나로 추상화합니다. VS Code·Zed·IntelliJ·Neovim·Emacs·Vim용 LSP, DAP 디버거, 포매터, MCP 서버, 주피터 스타일 노트북까지 갖춘 툴체인과 WebAssembly 빌드로 만든 브라우저 플레이그라운드를 보면, 언어 하나를 설계할 때 실행 엔진뿐 아니라 개발자 경험 전체를 함께 설계한 흔적이 뚜렷합니다. 게임 오브 라이프나 브레인퍽 인터프리터 같은 50개가 넘는 예제 프로그램으로 언어의 표현력을 검증해둔 점도 눈에 띕니다.
