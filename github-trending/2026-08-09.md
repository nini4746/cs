---
title: "GitHub Trending 2026-08-09"
tags: [github-trending]
---

오늘 트렌딩 최상위는 어제에 이어 PrimeIntellect-ai/prime-agent가 지키고 있는데, "세션 경계를 넘어 지속되는 에이전트"라는 주제가 이틀 연속 가장 큰 폭의 스타 증가를 이끌고 있습니다. 그 아래로는 코드베이스를 그래프 DB에 넣고 자연어로 질의하는 code-graph-rag, 230개가 넘는 전문 에이전트 페르소나 모음인 agency-agents가 나란히 오르며 "에이전트를 무엇으로 채울 것인가"라는 질문이 여전히 트렌딩의 큰 축을 이루고 있습니다. 동시에 프로세스·포트·컨테이너의 인과관계를 추적하는 시스템 진단 도구 witr와 구글 딥마인드의 기상 예측 모델 weathernext가 함께 상위권에 오르면서, AI 에이전트 화제 바깥에서도 실용적인 시스템 도구와 과학 연구 코드가 꾸준히 관심을 받고 있음을 보여줍니다.

## PrimeIntellect-ai/prime-agent

- <https://github.com/PrimeIntellect-ai/prime-agent>
- 언어: TypeScript
- 오늘 스타: 2,319 (총 10,240)

컨텍스트를 변수로, 도구를 함수 호출로 다루는 "재귀적 언어 모델(RLM)"과 세션을 넘어 상태를 유지하는 "Continual Harness"를 두 축으로 삼는 장기 자율 에이전트로, 어제에 이어 오늘도 트렌딩 최상단을 지켰습니다. 내장 IPython REPL 위에서 파일·셸 작업을 프로그래밍적으로 실행하고, `rlm(...)` 호출로 서브에이전트를 병렬·백그라운드로 띄우며, 터미널 연결이 끊겨도 데몬 기반 세션으로 재접속할 수 있는 구조가 핵심입니다. `/refine` 명령으로 과거 실행 궤적을 검토해 하네스만 점진적으로 갱신하는 방식은, 채팅창이 닫히면 맥락이 사라지는 에이전트의 한계를 세션 밖 영속 상태로 풀어보려는 시도로 볼 수 있습니다.

## msitarzewski/agency-agents

- <https://github.com/msitarzewski/agency-agents>
- 언어: Shell
- 오늘 스타: 446 (총 140,263)

엔지니어링·디자인·마케팅·세일즈·게임 개발·헬스케어 등 17개 분야에 걸쳐 230개가 넘는 전문 에이전트 페르소나를 모아둔 저장소로, 각 에이전트가 성격·업무 흐름·성공 지표·산출물까지 갖춘 것이 특징입니다. Claude Code, GitHub Copilot, Cursor, Windsurf, Gemini CLI 등 14개가 넘는 플랫폼에 그대로 설치되도록 변환 스크립트를 갖췄는데, 총 스타가 14만을 넘는 규모를 보면 "역할별 에이전트 페르소나를 표준 포맷으로 유통한다"는 접근이 스킬 파일 생태계 다음 단계로 자리잡고 있다는 신호로 읽힙니다.

## vitali87/code-graph-rag

- <https://github.com/vitali87/code-graph-rag>
- 언어: Python
- 오늘 스타: 59 (총 2,766)

Tree-sitter로 여러 언어의 코드베이스를 파싱해 함수·클래스·모듈과 그 관계를 Memgraph 그래프 DB에 저장하고, 자연어 질의를 Cypher 쿼리로 변환해 코드 검색과 AI 기반 수정까지 이어주는 RAG 시스템입니다. Python·TypeScript·Rust·Go·Java·C/C++ 등 다수 언어를 지원하면서 ast-grep 기반 구조적 검색, C#·Java·C·Go에 걸친 데이터 흐름(taint) 추적, 호출·참조 그래프를 이용한 데드 코드 탐지까지 갖췄고 Claude Code용 MCP 서버 연동도 제공합니다. 코드베이스를 텍스트가 아니라 그래프로 모델링했을 때 "이 함수를 누가 어떻게 부르는가" 같은 질의가 어떻게 쉬워지는지 보여주는 사례입니다.

## pranshuparmar/witr

- <https://github.com/pranshuparmar/witr>
- 언어: Go
- 오늘 스타: 556 (총 20,352)

"이게 왜 실행되고 있지?"라는 질문에 답하는 CLI/TUI 도구로, 프로세스 이름·PID·포트 번호·파일 핸들·컨테이너 이름 중 무엇으로 질의하든 그 프로세스가 어떻게 시작되어 지금까지 살아있는지 계보(ancestry chain)를 추적해 보여줍니다. Linux에서는 `/proc`을, macOS에서는 `ps`/`lsof`/`sysctl`을, Windows에서는 Win32 API를 직접 사용해 플랫폼별로 프로세스 트리를 구성하고, Docker·Podman·Kubernetes 등 여러 컨테이너 런타임까지 인식합니다. 표준 트리 뷰 외에 JSON 출력과 실시간 정렬·필터링이 가능한 TUI 대시보드까지 갖춰서, 운영체제가 프로세스 생성 관계를 어떻게 노출하는지 플랫폼별로 비교하며 공부하기 좋은 도구입니다.

## google-deepmind/weathernext

- <https://github.com/google-deepmind/weathernext>
- 언어: Python
- 오늘 스타: 55 (총 6,949)

구글 딥마인드의 전지구 중기 기상·태풍 예측 모델 저장소로, 그래프 신경망과 확산 기반 앙상블 기법을 함께 쓰는 최신 모델 WeatherNext 2(WN2)와 앞선 모델 GraphCast·GenCast의 코드를 함께 담고 있습니다. WN2는 ECMWF HRES 데이터로 파인튜닝한 0.25도(약 30km) 해상도 결정론적 예측 모델이고, 자원이 제한된 환경을 위한 1도 해상도 경량 태풍 예측 모델도 별도로 제공합니다. 자기회귀적 롤아웃, 입력 정규화, 그래프 구성 요소, JAX 호환 xarray 유틸리티 등을 공유 모듈로 분리해둔 구조를 보면, 대형 기상 예측 모델을 실제 서비스(WeatherLab, OpenMeteo)로 내보내는 파이프라인이 어떻게 조직되는지 참고할 수 있습니다.

## 오늘의 히든잼

### GV4F/ND7.db

- <https://github.com/GV4F/ND7.db>
- 언어: C#
- 스타: 5 (2026-08-05 생성)

Bitcask에서 영감을 받은 로그 구조 아키텍처로 만든 임베더블 키-값 저장소로, TCP 서버와 클라이언트 라이브러리를 모두 제공합니다. 메모리에는 키와 디스크상의 위치(오프셋·크기)만 담은 해시 테이블을 두어 키-값 쌍당 O(1) 메모리만 쓰고, 모든 쓰기는 활성 로그 파일 끝에 순차적으로만 추가해 랜덤 디스크 탐색을 없앴습니다. 재시작 시에는 바이너리 로그를 처음부터 순차 스캔해 인메모리 인덱스를 재구성하며, `SemaphoreSlim` 기반의 세밀한 동시성 제어와 임시 파일을 이용한 무중단 로그 로테이션까지 외부 라이브러리 없이 순수 .NET만으로 구현했습니다. 50개 동시 클라이언트에서 초당 54,117건, P99 지연 40ms 미만이라는 벤치마크 수치와 30만 건 스트레스 테스트 도구까지 갖춘 점에서, 로그 구조 저장소의 핵심 아이디어를 작은 코드베이스로 온전히 구현한 참고 자료로 좋습니다.

### OlegSotnikov/jc-rs

- <https://github.com/OlegSotnikov/jc-rs>
- 언어: Rust
- 스타: 103 (2026-08-04 생성)

명령줄 도구의 출력을 JSON으로 바꿔주는 파이썬 도구 `jc`를 Rust로 재구현한 프로젝트로, 934개의 개별 파서를 jc-rs-core(파서 트레잇·레지스트리)·jc-rs-parsers(실제 파서 구현)·jc-rs-utils(공용 유틸)로 나눈 모듈 구조를 갖췄습니다. 파서들이 링크 타임에 스스로 레지스트리에 등록되어 수동 등록 없이 런타임에 조회되는 구조이고, jc v1.25.7의 934개 "오라클" 픽스처 쌍 전체에 대해 원본과 100% 동일한 출력을 내는지 CI에서 강제하는 차등 테스트를 갖췄습니다. "jc가 정답이고, 둘이 다르면 jc-rs의 버그"라는 원칙으로 스키마를 임의로 바꾸지 않으면서도, 콜드 스타트 21.6배, 대량 처리 3.2~6.4배 속도 향상과 NDJSON 스트리밍까지 얹었습니다. 기존 도구를 다른 언어로 재구현할 때 "원본과의 완전한 호환"을 테스트로 어떻게 증명하는지 보여주는 좋은 사례입니다.

### Shivfun99/Pulse-Order

- <https://github.com/Shivfun99/Pulse-Order>
- 언어: C++
- 스타: 120 (2026-06-02 생성)

DPDK로 패킷을 직접 받아 처리하는 C++20 저지연 트레이딩 엔진으로, 저자 스스로 "트레이딩 엔진의 핫 패스를 배우고 증명하는 것이 목적"이라고 밝히고 있습니다. 시장 데이터 패킷을 디코드해 인메모리 오더북을 갱신하고, 매수·매도 압력 불균형만 보는 단순 전략으로 신호를 낸 뒤 리스크 체크를 거쳐 주문을 인코딩해 내보내는 선형 파이프라인으로, 측정 구간에서는 힙 할당·로깅·문자열 포매팅을 전혀 쓰지 않도록 설계했습니다. 100만 건의 합성 이벤트에 대한 마이크로초 단위 벤치마크와 veth 가상 인터페이스를 이용한 기능 테스트를 갖췄고, 측정한 지연이 애플리케이션 처리 구간만을 나타낼 뿐 실제 NIC·거래소 지연은 포함하지 않는다고 스스로 명시한 점까지 포함해, 저지연 시스템을 벤치마킹할 때 무엇을 측정하고 무엇을 측정하지 않았는지 정직하게 구분하는 태도를 보여주는 자료입니다.
