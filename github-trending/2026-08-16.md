---
title: "GitHub Trending 2026-08-16"
tags: [github-trending]
---

오늘 트렌딩은 "직접 밑바닥부터 다시 만든다"는 태도가 눈에 띄는 하루입니다. DHH가 만든 오피니언이 뚜렷한 Linux 배포판(omarchy), 웹·데스크톱·모바일을 하나의 Rust 코어로 다시 짜는 비디오 에디터(OpenCut)가 나란히 상위권에 올랐고, 어제 1위였던 Node.js 메타프레임워크 cordis도 계속 자리를 지키고 있습니다. 여기에 서로 다른 코딩 에이전트를 오가도 맥락을 잃지 않게 해주는 메모리 시스템(ai-memory)과 로컬 파인튜닝 가속 라이브러리(unsloth)가 더해지면서, 개발 환경 자체를 원하는 대로 다시 설계하려는 흐름과 에이전트 워크플로를 뒷받침하는 인프라에 대한 관심이 동시에 읽히는 하루입니다.

## basecamp/omarchy

- <https://github.com/basecamp/omarchy>
- 언어: Shell
- 오늘 스타: 225 (총 25,169)

DHH(37signals 창업자)가 만든 "아름답고 오피니언이 뚜렷한" Linux 배포판으로, Hyprland 기반 데스크톱 환경 위에 Neovim, 터미널 도구, 테마, 설치·마이그레이션 스크립트를 하나로 묶어 제공합니다. 6천 개가 넘는 커밋과 50개 이상의 챕터로 이뤄진 매뉴얼을 갖추고 있어, macOS·Windows에서 넘어오는 개발자가 설정에 시간을 쓰지 않도록 기본값 자체를 세심하게 설계한 점이 특징입니다. 배포판 전체를 셸 스크립트로 구성해 설치·테마·패키지 관리를 어떻게 자동화하는지 참고하기 좋은 프로젝트입니다.

## unslothai/unsloth

- <https://github.com/unslothai/unsloth>
- 언어: Python
- 오늘 스타: 580 (총 72,331)

LLM 파인튜닝을 개인용 하드웨어에서도 돌릴 수 있게 가속하는 라이브러리로, RoPE·MLP 연산에 직접 짠 Triton 커널과 QLoRA·FP8 양자화를 조합해 표준 대비 2배 빠른 속도와 70% 적은 VRAM 사용을 내세웁니다. Tauri 기반 데스크톱 앱과 웹 UI(Unsloth Studio)까지 갖추며 Python 라이브러리를 넘어 로컬 AI 워크벤치로 영역을 넓히고 있고, llama.cpp와 연동해 GGUF·NVFP4 등 다양한 양자화 포맷으로 내보낼 수 있습니다. 커널 레벨 최적화로 학습 비용을 낮추는 구체적인 기법을 공부하기 좋은 프로젝트입니다.

## OpenCut-app/OpenCut

- <https://github.com/OpenCut-app/OpenCut>
- 언어: TypeScript
- 오늘 스타: 134 (총 83,621)

CapCut의 오픈소스 대안을 표방하는 비디오 편집기로, 기존 버전(opencut-classic)을 유지하면서 웹·데스크톱·모바일을 하나의 Rust 코어로 구동하는 완전한 재작성을 진행 중입니다. 서드파티 확장을 위한 플러그인 아키텍처, 프로그래밍 방식 접근을 위한 Editor API, AI 에이전트를 위한 MCP 서버 연동까지 계획하고 있어, 프로덕트 UI 계층과 별개로 멀티플랫폼 코어 엔진을 어떻게 설계하는지 지켜볼 만한 프로젝트입니다. 8만 개가 넘는 스타를 가진 프로젝트가 처음부터 다시 짜는 결정을 내렸다는 점 자체가, 크로스플랫폼 미디어 편집기의 아키텍처적 난이도를 보여줍니다.

## cordiverse/cordis

- <https://github.com/cordiverse/cordis>
- 언어: TypeScript
- 오늘 스타: 719 (총 4,525)

"시공간적 조합 가능성(Spatiotemporal Composability)"을 내세우는 Node.js 메타프레임워크로, 어제 1위에 이어 오늘도 큰 폭으로 스타가 늘며 상위권을 지켰습니다. 플러그인과 서비스를 이펙트 기반으로 동적으로 등록·해제할 수 있는 구조를 핵심으로 하며, 여러 패키지로 나뉜 모노레포에 테스트 커버리지 인프라까지 갖춘 점이 눈에 띕니다. API가 아직 안정화되지 않은 실험적 단계라고 스스로 밝히면서도 이틀 연속 트렌딩 1위권을 지키는 것을 보면, 의존성 주입과 생명주기 관리를 새로운 패러다임으로 재정의하려는 시도에 관심이 꾸준하다는 뜻으로 보입니다.

## akitaonrails/ai-memory

- <https://github.com/akitaonrails/ai-memory>
- 언어: Rust
- 오늘 스타: 41 (총 1,626)

"Claude Code를 쓰다가 중간에 멈추고 같은 디렉터리에서 OpenAI Codex를 시작해도 아키텍처를 다시 설명할 필요가 없게" 해주는, 코딩 에이전트 간 장기 기억 시스템입니다. Git으로 버전 관리되는 마크다운 위키를 SQLite FTS5로 인덱싱하고, 세션 관찰 내용을 그대로 저장하는 대신 일관된 페이지로 "컴파일"한 뒤 FTS5·엔티티 매칭·그래프 이웃 RRF·벡터 임베딩을 섞은 하이브리드 랭킹으로 검색하는 구조가 특징입니다. Rust 단일 바이너리로 MCP 엔드포인트와 HTTP API를 함께 제공해 Claude Code, Codex, Devin 등 여러 에이전트를 오가며 컨텍스트를 유지하려는 실질적 필요를 다루고 있어, 에이전트 간 상태를 어떻게 표준화해 넘길지 참고할 만합니다.

## 오늘의 히든잼

### AngelOnFira/weblings

- <https://github.com/AngelOnFira/weblings>
- 언어: Rust, 스타: 101, 생성일: 2026-07-22

"WASM 안에서 Rust를 WASM으로 컴파일한다"는 발상을 실제로 구현한 프로젝트로, `rustc`를 패치해 브라우저 안에서 직접 Rust 코드를 컴파일·실행할 수 있게 만들었습니다. `rustc -> Cranelift IR -> waffle -> wasm 오브젝트 파일 -> 링커 -> wasm 실행 파일`이라는 파이프라인을 직접 구성했고, LLVM 대신 더 가볍고 빠른 Cranelift를 백엔드로 선택한 이유까지 문서에 밝혀두고 있습니다. Rustlings 연습문제 세트와 플레이그라운드를 웹 UI로 제공해 실제로 동작을 확인할 수 있게 해둔 점도 눈에 띄며, 컴파일러 백엔드와 WASM 링커를 처음부터 새로 짜는 과정을 들여다볼 수 있어 선정했습니다.

### kisasexypantera94/varmint

- <https://github.com/kisasexypantera94/varmint>
- 언어: Rust, 스타: 33, 생성일: 2026-05-07

Apple Silicon Mac에서 Windows·Linux 게임을 돌리기 위한 경량 가상머신으로, macOS의 Hypervisor.framework 위에 Debian ARM64 게스트와 FEX(x86 호환 레이어), Proton을 얹은 구조입니다. 그래픽 파이프라인은 게스트의 Vulkan/OpenGL 호출을 Mesa와 virglrenderer를 거쳐 MoltenVK·ANGLE로 넘기고 최종적으로 Metal이 하드웨어 가속을 처리하는 여러 겹의 변환 구조로 되어 있고, 클립보드 공유·네트워킹·오디오·고주사율 디스플레이까지 지원 범위를 문서화해뒀습니다. 11개 이상의 게임에 대한 호환성 표와 실제 플레이 영상으로 동작을 검증해둔 점, UTM 프로젝트가 관리하는 포크들을 기반으로 삼았다는 점에서 커뮤니티 검증을 거친 가상화 스택이라 판단해 선정했습니다.

### wgli-collab/qs-arkts

- <https://github.com/wgli-collab/qs-arkts>
- 언어: TypeScript, 스타: 45, 생성일: 2026-06-17

JavaScript 생태계에서 널리 쓰이는 querystring 파서 `qs`(v6.15.2)를 HarmonyOS ArkTS로 포팅한 라이브러리입니다. Node.js의 `Buffer` 연산을 ArkTS의 `TextEncoder`/`TextDecoder`로 바꾸는 등 플랫폼 제약에 맞게 원본 로직을 옮기면서도, 중첩 객체·배열의 여러 표현 형식(indices, brackets, repeat, comma), RFC 1738/3986 인코딩, 프로토타입 오염 방지 가드까지 원본의 기능을 빠짐없이 재현했습니다. 파싱·직렬화·인코딩·보안 가드를 모듈별로 분리한 구조와 테스트 모듈을 갖추고 있어, 잘 알려진 JS 라이브러리를 다른 런타임으로 이식할 때 무엇을 신경 써야 하는지 보여주는 사례로 선정했습니다.

### MkP369/idu

- <https://github.com/MkP369/idu>
- 언어: C++, 스타: 26, 생성일: 2026-06-19

`du -sh`보다 빠른 디스크 사용량 분석기를 목표로 하는 C++20 CLI 도구로, 멀티스레드로 디렉터리 트리를 병렬 순회하고 `statx` 시스템 콜로 메타데이터를 효율적으로 읽어옵니다. README에 실린 벤치마크에 따르면 약 60GB 데이터의 핫 캐시 기준 `du -sh`가 533.5ms 걸리는 작업을 66.1±2.6ms에 끝냈다고 밝히고 있어, 병렬화와 시스템 콜 선택만으로 얼마나 성능 차이가 나는지 구체적인 수치로 보여줍니다. 유닛 테스트는 아직 TODO로 남아 있다고 스스로 밝힌 점은 아쉽지만, 파일시스템 순회 알고리즘을 밑바닥부터 최적화하는 과정을 들여다보기 좋아 선정했습니다.
