---
title: "GitHub Trending 2026-07-22"
tags: [github-trending]
---

오늘 트렌딩은 오래된 것과 최신 AI 인프라가 나란히 상위권을 차지한 하루입니다. 500개 넘는 뉴스 피드를 모으는 지정학 모니터링 대시보드(worldmonitor)가 이틀 연속 압도적 1위를 지켰고, 코딩 에이전트의 말투 자체를 바꾸는 스킬(i-have-adhd)과 268개가 넘는 AI 모델 제공자를 하나의 엔드포인트로 묶는 게이트웨이(OmniRoute)가 뒤를 이으면서 "에이전트를 어떻게 다루고 연결할 것인가"라는 주제가 계속 이어지고 있습니다. 그 사이에 카메라 없이 WiFi 신호만으로 생체 신호를 읽어내는 하드웨어 프로젝트(RuView)와, 1969년 달 착륙을 이끈 오리지널 어셈블리 코드 저장소(Apollo-11)가 함께 오르면서, 최신 AI 인프라 열풍과 반세기 전 임베디드 소프트웨어 유산이 한 화면에 공존하는 흥미로운 대비를 보여줍니다.

## koala73/worldmonitor

- <https://github.com/koala73/worldmonitor>
- 언어: TypeScript
- 오늘 스타: 4,131 (총 68,157)

지정학·금융·인프라 신호를 하나의 대시보드로 모으는 실시간 글로벌 모니터링 프로젝트로, 어제에 이어 오늘도 트렌딩 전체 1위를 지켰습니다. Vite 기반 TypeScript 프런트엔드에 deck.gl·globe.gl로 3D 지구본과 WebGL 지도를 함께 렌더링하고 Tauri 2로 데스크톱 앱까지 뽑아내며, Ollama·Groq 연동으로 로컬에서도 AI 분석이 돌아가게 만든 구조입니다. 이틀 만에 5천 개에 가까운 스타가 붙은 것을 보면, 정보 과부하 시대에 여러 데이터 소스를 하나의 시각화로 압축해 보여주는 수요가 꾸준하다는 뜻으로 읽히며, 대규모 실시간 파이프라인과 지도 시각화 기법을 함께 공부하기 좋은 프로젝트입니다.

## ayghri/i-have-adhd

- <https://github.com/ayghri/i-have-adhd>
- 언어: Python
- 오늘 스타: 1,682 (총 7,926)

코딩 에이전트가 장황한 설명 대신 실행 가능한 다음 행동부터 제시하도록 답변 스타일을 강제하는 스킬로, 어제에 이어 오늘도 상위권을 지켰습니다. Claude Code·Codex 등 여러 에이전트에 꽂을 수 있는 플러그인 구조로 만들어졌고 Python으로 규칙을 구현했는데, 이틀 연속 스타가 크게 늘어난 것을 보면 "무엇을 할지"보다 "어떻게 말할지"를 통제하는 스킬 수요가 여전히 크다는 신호로 보입니다. 코드가 아니라 에이전트의 커뮤니케이션 스타일 자체를 스킬 파일로 다루는 방식은, 프롬프트 엔지니어링의 대상이 절차뿐 아니라 어조까지 넓어지고 있음을 보여주는 사례입니다.

## diegosouzapw/OmniRoute

- <https://github.com/diegosouzapw/OmniRoute>
- 언어: TypeScript
- 오늘 스타: 1,648 (총 약 24,700)

278개가 넘는 AI 모델 제공자를 하나의 엔드포인트로 묶어주는 오픈소스 AI 게이트웨이로, Node.js 22+와 Next.js 16 위에 SQLite로 상태를 관리하고 MCP·A2A 에이전트 프로토콜을 함께 구현했습니다. 11개 엔진으로 이루어진 압축 파이프라인으로 토큰을 최대 95%까지 줄이고, 서킷 브레이커와 지수 백오프로 장애 시 자동 전환을 처리하는 복원력 계층이 핵심 구조입니다. npm 전역 설치부터 Docker, Electron 데스크톱 앱, 심지어 Termux를 통한 Android 실행까지 지원하는 폭넓은 배포 방식은, 여러 AI 제공자를 안정적으로 오가야 하는 실무 환경에서 라우팅과 장애 복구를 어떻게 설계하는지 참고하기 좋습니다.

## ruvnet/RuView

- <https://github.com/ruvnet/RuView>
- 언어: Rust
- 오늘 스타: 875 (총 약 83,300)

카메라 없이 WiFi 신호만으로 존재 감지, 호흡·심박 같은 생체 신호 측정, 움직임 추적까지 해내는 프로젝트입니다. 9달러짜리 ESP32 센서로 Channel State Information(CSI)을 수집한 뒤 56개 서브캐리어를 다중 대역 융합하고, Hampel 필터링과 스펙트로그램으로 신호를 처리한 다음 신경망으로 자세 추정과 생체 신호를 뽑아내는 파이프라인 구조입니다. 엣지 펌웨어·신호 처리는 Rust로, 학습 파이프라인은 Python으로 나눠 구현한 점에서, 무선 신호를 신경망 입력으로 바꾸는 전체 과정과 privacy-preserving 센싱이라는 주제를 함께 공부하기 좋은 프로젝트입니다.

## chrislgarry/Apollo-11

- <https://github.com/chrislgarry/Apollo-11>
- 언어: Assembly
- 오늘 스타: 766 (총 약 70,400)

1969년 달 착륙을 이끈 아폴로 11호 유도 컴퓨터(AGC)의 오리지널 소스 코드 저장소로, MIT 박물관에 보관된 종이 문서를 디지털화한 Virtual AGC 프로젝트의 자료를 바탕으로 사령선(Comanche055)과 착륙선(Luminary099) 코드를 모두 담고 있습니다. 전체가 어셈블리어로 작성된 실제 비행 소프트웨어라는 점에서, 반세기 전 임베디드 시스템이 극도로 제한된 메모리와 연산 자원 안에서 우주선 유도·항법·제어를 어떻게 구현했는지 원본 그대로 들여다볼 수 있습니다. 오늘 트렌딩에 함께 오른 최신 AI 인프라 프로젝트들과 나란히 놓고 보면, 소프트웨어 엔지니어링의 제약 조건이 시대에 따라 얼마나 달라졌는지 체감하기 좋은 자료입니다.

## 오늘의 히든잼

### RightNow-AI/auto

- <https://github.com/RightNow-AI/auto>
- 언어: Rust
- 스타: 116
- 생성일: 2026-07-05

기록된 LLM 에이전트의 행동 트레이스를 관찰해 결정론적으로 반복되는 패턴을 찾아내고, 이를 샌드박스 WebAssembly 바이너리로 컴파일해 프런티어 모델 호출 없이 실행하게 만드는 "AGI 컴파일러"입니다. Frontend(트레이스 수집) → IR(타입이 있는 태스크 그래프) → 최적화 패스 → Backend(wasmtime 실행) 구조를 갖췄고, 낯선 입력은 trigram-Jaccard 거리와 split-conformal 보정을 쓰는 가드가 감지해 다시 프런티어 모델로 넘기는 "디옵트 후 재컴파일" 방식을 씁니다. 벤치마크에서 항목당 비용이 프런티어 모델 대비 약 6.4배 줄었다는 수치와 함께 "48.9%가 느슨한 보정 아래서 조용히 실패한다"는 한계까지 솔직히 밝혀둔 점에서, 에이전트 실행을 어떻게 점진적으로 컴파일해 비용을 낮추는지 공부하기 좋은 프로젝트입니다.

### OpenLoadBalancer/olb

- <https://github.com/OpenLoadBalancer/olb>
- 언어: Go
- 스타: 25
- 생성일: 2026-03-13

표준 라이브러리와 golang.org/x 패키지만으로 만든 L4/L7 로드 밸런서로, HTTP·WebSocket·gRPC·TCP·UDP를 모두 처리하며 16가지 로드 밸런싱 알고리즘과 6단계 WAF(SQLi·XSS·경로 순회 탐지 포함), Raft 클러스터링과 SWIM 가십 프로토콜로 분산 운영까지 구현했습니다. "Listeners → Middleware → Router → Balancer → Backends"로 이어지는 계층 구조가 핵심이며, 70개 패키지에 걸쳐 56개 이상의 엔드투엔드 테스트와 95.3% 커버리지를 유지하고 초당 1만 5천 회 이상의 처리량을 벤치마크로 검증했다고 밝히고 있습니다. 외부 프레임워크 없이 표준 라이브러리만으로 프로덕션급 로드 밸런서를 밑바닥부터 구현한 사례로, 네트워킹과 분산 합의를 함께 공부하기 좋습니다.

### nvrmnd-png/undo

- <https://github.com/nvrmnd-png/undo>
- 언어: Rust
- 스타: 51
- 생성일: 2026-07-15

셸에서 `mv`, `rm`, `cp`, `chmod` 같은 파괴적 명령을 가로채 SQLite에 저널로 남기고, 필요하면 되돌릴 수 있게 해주는 도구입니다. 삭제된 파일은 영구히 지워지는 대신 freedesktop 휴지통으로 이동하며, BLAKE3로 파일 내용을 지문화해 명령 실행 이후 내용이 바뀐 파일은 `--force` 없이는 복원을 거부하는 무결성 검사를 갖췄습니다. zsh·bash·fish 셸 통합과 대화형 TUI 브라우저까지 제공하고 150개가 넘는 단위·통합 테스트로 검증했다고 밝히는 점에서, 되돌릴 수 있는 셸 연산을 파일시스템 계층에서 어떻게 안전하게 설계하는지 참고하기 좋은 프로젝트입니다.

### cocoonstack/cocoon-macos

- <https://github.com/cocoonstack/cocoon-macos>
- 언어: Go
- 스타: 44
- 생성일: 2026-05-31

x86 Linux/KVM 호스트에서 실제 macOS(Sequoia 15, Tahoe 26)를 게스트로 돌리는 가상 머신 엔진으로, QEMU와 OpenCore 부트로더, OVMF 펌웨어를 조합해 구현했습니다. `create`, `run`, `start`, `stop` 같은 Docker와 비슷한 CLI를 제공하고, CNI로 실제 LAN IP를 DHCP로 할당하며 qcow2 이미지에 스냅샷과 copy-on-write 클론을 지원하는 구조가 특징입니다. GitHub Actions로 골든 이미지를 자동 빌드하고 sha256 검증까지 곁들인 이미지 배포 파이프라인을 갖춘 점에서, VM 이미지 생명주기 관리와 하드웨어 가상화를 함께 공부하기 좋은 프로젝트입니다.

### dweinstein/canary

- <https://github.com/dweinstein/canary>
- 언어: Go
- 스타: 95
- 생성일: 2026-03-24

macOS에서 `.env`, `id_rsa`, `credentials.json` 같은 가짜 크리덴셜 파일을 지정한 마운트 지점에 심어두고, 누군가 이를 읽으면 즉시 알림을 띄우는 파일시스템 허니팟입니다. 커널 확장 없이 유저스페이스에서 동작하며, WebDAV 모드는 macOS 기본 `mount_webdav`로 현재 사용자 권한에서 돌아가고 NFS 모드는 Sun RPC/XDR로 NFSv3·MOUNT 프로토콜을 직접 구현해 루트 권한으로 비특권 사용자에게는 보이지 않게 만들 수 있습니다. 서드파티 의존성 없이 약 1,400줄의 Go 표준 라이브러리만으로 작성됐고 `lsof`로 접근 프로세스를 식별해 30초 창으로 알림 중복을 걸러내는 등, 침해 탐지를 파일시스템 계층에서 가볍게 구현하는 법을 살펴보기 좋은 도구입니다.
