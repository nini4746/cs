---
title: "GitHub Trending 2026-07-27"
tags: [github-trending]
---

오늘 트렌딩은 "중앙 서버 없이 어떻게 신뢰를 만들 것인가"라는 질문이 여러 형태로 반복되는 하루입니다. 계정도 전화번호도 없이 블루투스 메시와 Nostr 릴레이만으로 메시지를 주고받는 bitchat이 압도적인 스타 증가폭으로 1위에 올랐고, 바로 옆에는 자기 서버에 VPN을 직접 구축하게 해주는 amnezia-client가 나란히 트렌딩에 올라 있어 "인프라를 개인이 직접 소유한다"는 흐름이 뚜렷합니다. 여기에 터미널 작업 경험을 다듬는 superfile, 소셜 미디어 데이터를 브라우저 자동화로 수집하는 MediaCrawler, AI 가상 동반자를 로컬에서 돌리는 airi까지 겹치면서, 탈중앙화·자기 호스팅·로컬 우선이라는 키워드가 오늘 트렌딩 전체를 관통합니다.

## permissionlesstech/bitchat

- <https://github.com/permissionlesstech/bitchat>
- 언어: Swift
- 오늘 스타: 2,344 (총 31,800)

계정·전화번호·중앙 서버 없이 동작하는 블루투스 메시 채팅 앱입니다. 오프라인에서는 BLE로 최대 7홉까지 메시지를 릴레이하며 Noise 프로토콜로 종단간 암호화를 걸고, 인터넷이 있을 때는 약 290개의 Nostr 릴레이를 통해 geohash 기반 위치 채널로 전역 통신까지 확장하는 이중 전송 구조가 핵심입니다. 로컬 무선 메시 네트워크를 실제로 구현하면서 동시에 기존 프로토콜(Nostr)을 확장해 오프라인/온라인을 매끄럽게 전환하는 설계를 공부하기 좋은 사례입니다.

## amnezia-vpn/amnezia-client

- <https://github.com/amnezia-vpn/amnezia-client>
- 언어: C++
- 오늘 스타: 515 (총 13,600)

자기 서버에 직접 VPN을 구축할 수 있게 해주는 셀프 호스팅 VPN 클라이언트입니다. OpenVPN·WireGuard·IKEv2 같은 표준 프로토콜뿐 아니라 Cloak, Shadowsocks, AmneziaWG, XRay 같은 트래픽 위장(obfuscation) 레이어까지 함께 지원해, 검열이 심한 네트워크 환경에서 어떻게 프로토콜을 감추는지 살펴볼 수 있습니다. Qt와 CMake/Conan 기반으로 데스크톱·모바일을 아우르는 크로스플랫폼 빌드 구조도 참고할 만합니다.

## moeru-ai/airi

- <https://github.com/moeru-ai/airi>
- 언어: TypeScript
- 오늘 스타: 554 (총 43,800)

로컬에서 돌아가는 AI 가상 동반자를 만드는 오픈소스 프레임워크로, Vue와 WebGPU·WebAudio를 기반으로 브라우저·데스크톱(Electron)·모바일(PWA)을 모두 지원합니다. 30개 이상의 LLM 백엔드를 추상화 레이어(xsAI)로 묶고, VRM/Live2D 아바타 렌더링과 Minecraft·Factorio 같은 게임 연동까지 포함하는 구조라 "LLM을 실시간 음성·시각 인터랙션 파이프라인에 어떻게 통합하는가"를 폭넓게 살펴보기 좋습니다.

## yorukot/superfile

- <https://github.com/yorukot/superfile>
- 언어: Go
- 오늘 스타: 600 (총 20,700)

Bubble Tea 프레임워크로 만든 터미널 파일 매니저로, 플러그인과 테마를 지원하는 TUI 애플리케이션을 Go로 어떻게 구성하는지 보여주는 좋은 예제입니다. 표준 라이브러리만으로 파일시스템 연산을 처리하면서도 시각적으로 풍부한 TUI를 만드는 구조라, Bubble Tea 기반 도구를 직접 만들어보고 싶을 때 참고할 코드베이스로 적합합니다.

## NanmiCoder/MediaCrawler

- <https://github.com/NanmiCoder/MediaCrawler>
- 언어: Python
- 오늘 스타: 349 (총 58,000)

Xiaohongshu, TikTok, Bilibili, Weibo 등 여러 소셜 플랫폼의 공개 데이터를 수집하는 멀티 플랫폼 크롤러입니다. 서명 알고리즘을 직접 리버스 엔지니어링하는 대신 Playwright로 실제 로그인된 브라우저 컨텍스트를 유지하며 필요한 서명 파라미터를 JS로 그대로 뽑아 쓰는 접근이 특징으로, "복잡한 안티 크롤링을 코드 분석 대신 브라우저 자동화로 우회한다"는 실용적인 전략을 배울 수 있습니다.

## 오늘의 히든잼

### aicheye/crustty

- <https://github.com/aicheye/crustty>
- 언어: Rust
- 스타: 55 (2026-02-08 생성)

시간여행 디버깅을 지원하는 교육용 C 인터프리터입니다. 손으로 짠 recursive descent 파서(연산자 우선순위는 precedence climbing)로 AST를 만들고, 스택 기반 인터프리터가 이를 실행하면서 스택은 `0x0000_0004`, 힙은 `0x7fff_0000`부터 시작하는 식으로 주소 공간을 분리해 포인터 종류를 값만으로 구분하는 설계가 눈에 띕니다. 매 statement 실행 후 스택·힙·출력·소스 위치 전체를 스냅샷으로 남겨 앞뒤로 오갈 수 있는 time-travel 시스템을 ratatui TUI 위에 올렸고, 유닛·통합 테스트와 함께 "프로덕션급이 아니다", "전처리기가 없다" 같은 한계를 README에 솔직하게 밝혀둔 점에서 완성도가 높습니다.

### xs-lang0/xs

- <https://github.com/xs-lang0/xs>
- 언어: C
- 스타: 127 (2026-03-24 생성)

바이트코드 VM, 트리워킹 인터프리터, x86-64/aarch64 JIT, C·JS 트랜스파일, WASM 런타임까지 하나의 정적 링크 바이너리 안에 여러 실행 백엔드를 모두 갖춘 언어 툴체인입니다. 컴파일러·언어 서버·디버거·포매터·린터·테스트 러너·패키지 매니저를 한 바이너리로 제공한다는 목표 아래, 매 커밋마다 인터프리터와 VM의 출력을 diff로 대조하는 크로스 백엔드 검증과 behavioral·adversarial·conformance·fuzz 테스트를 포함한 7단계 테스트 레이어를 갖추고 있어, 여러 실행 전략을 한 언어 구현 안에서 비교하며 공부하기 좋은 프로젝트입니다.

### RamakrishnaChilaka/FerrisSearch

- <https://github.com/RamakrishnaChilaka/FerrisSearch>
- 언어: Rust
- 스타: 14 (2026-03-16 생성)

전문 검색과 SQL 분석을 결합한 분산 검색 엔진으로, Tantivy(검색)·Arrow(컬럼 데이터)·DataFusion(SQL 엔진)·openraft(합의)·USearch(벡터 검색)·tonic(gRPC)을 조합해 JVM 없이 단일 바이너리로 동작합니다. Raft가 클러스터 멤버십과 메타데이터를 담당하고, WAL과 Tantivy 인덱스를 다루는 mutable 데이터 플레인 위에서 쿼리 필터링을 샤드 쪽으로 밀어넣은 뒤 나머지 관계형 연산만 DataFusion이 처리하는 계층 분리 설계가 핵심입니다. "검색으로 먼저 좁히고 그 결과만 네트워크로 옮긴다"는 철학을 명확히 문서화했고, pre-1.0 상태에서 남은 한계(replica bootstrap, 동시성 제어 미완성 등)까지 솔직히 밝힌 점이 신뢰도를 높입니다.

### BenChaliah/Tensa-Lang

- <https://github.com/BenChaliah/Tensa-Lang>
- 언어: C++
- 스타: 77 (2026-02-04 생성)

LLM 추론 엔진을 위한 텐서 우선 언어로, attention·정규화·샘플링 같은 모델 로직을 `.tl` 소스로 작성하면 MLIR을 거쳐 CPU(LLVM JIT)와 CUDA(NVVM + cuBLAS)로 컴파일됩니다. `with tile=... parallel=... memory=...` 같은 힌트로 스케줄링을 소스 코드 레벨에 직접 노출한 점이 특징이며, S-expression IR을 매개로 서피스 문법을 MLIR linalg/scf/arith/memref 연산으로 낮추는 다단계 컴파일 파이프라인 구조가 명확히 문서화돼 있습니다. Llama2·Qwen2.5 같은 실제 모델을 예제로 함께 제공해 파이프라인 전체를 따라가 볼 수 있지만, 자동화된 테스트 스위트는 아직 눈에 띄지 않아 검증은 예제 실행에 의존하는 상태입니다.
