---
title: "GitHub Trending 2026-09-13"
tags: [github-trending]
---

오늘 트렌딩은 에이전트가 손대는 영역이 계속 넓어지고 있다는 인상을 다시 확인시켜 줍니다. 위성·항공기·선박 데이터를 실시간으로 3D 지구본에 얹는 시각화 프로젝트가 압도적인 스타 증가폭으로 1위를 차지했고, 바로 이어서 "AI에게 무엇을 시켜도 되는가"의 반대편 질문—AI가 실제로 어떤 지시를 받고 움직이는지—를 다루는 시스템 프롬프트 모음집이 뒤를 이었습니다. 여기에 소비자용 하드웨어로 수백억~수조 파라미터급 MoE 모델을 돌리는 추론 엔진, 침투 테스트를 스스로 수행하는 보안 에이전트, 자연어 한 줄로 영상 제작 파이프라인 전체를 지휘하는 도구까지 겹치면서, 에이전트가 코드 실행을 넘어 인프라 최적화·보안 진단·콘텐츠 제작까지 대신하는 흐름이 오늘 상위권 전반에 걸쳐 뚜렷하게 드러납니다.

## bilawalsidhu/gods-eye-view

- <https://github.com/bilawalsidhu/gods-eye-view>
- 언어: JavaScript
- 오늘 스타: 2,898 (총 31,194)

실시간 항공기·선박·위성·산불·지진·CCTV 데이터를 하나의 3D 지구본 위에 겹쳐 보여주는 시각화 프로젝트입니다. CesiumJS와 Google Photorealistic 3D Tiles로 지형을 렌더링하고, OpenAI Realtime API로 음성 명령을 받아 특정 항공편을 콕핏 뷰로 따라가거나 지도를 조작할 수 있습니다. API 키 없이 바로 실행 가능한 완전 오픈소스라는 점과, 이미 공개돼 있던 공공 데이터를 "정보기관 지휘통제실처럼" 시각화한 연출이 맞물려 폭발적인 관심을 끈 것으로 보입니다. 여러 공공 API를 하나의 실시간 지도 UI로 통합하는 아키텍처, 그리고 음성 인터페이스를 지도 탐색에 결합하는 방식을 참고하기 좋은 프로젝트입니다.

## asgeirtj/system_prompts_leaks

- <https://github.com/asgeirtj/system_prompts_leaks>
- 언어: JavaScript
- 오늘 스타: 727 (총 65,807)

Claude, ChatGPT, Gemini, Grok 등 주요 AI 챗봇들이 첫 메시지를 받기 전에 이미 부여받는 시스템 프롬프트를 유출·수집해 제공사별로 정리한 저장소입니다. Claude Code, Gemini Flash 같은 특정 제품 변형판 프롬프트까지 세분화해 담고 있어, 언론사들이 AI의 행동 규칙을 분석하는 자료로도 활용했다고 합니다. 에이전트에게 점점 더 많은 자율성을 부여하는 오늘 트렌딩의 다른 프로젝트들과 나란히 보면, "에이전트가 어떤 규칙 아래 움직이는지 투명하게 들여다보고 싶다"는 반대 방향의 관심이 꾸준히 크다는 것을 보여줍니다.

## JustVugg/colibri

- <https://github.com/JustVugg/colibri>
- 언어: C
- 오늘 스타: 652 (총 29,137)

7,440억~2.8조 파라미터급 MoE(mixture-of-experts) 모델을 GPU 없이 소비자용 하드웨어에서 돌리는 추론 엔진입니다. VRAM·RAM·디스크를 하나의 메모리 계층으로 취급해, 상시 필요한 덴스 레이어(약 170억 파라미터)만 int4로 RAM에 상주시키고 나머지 19,456개 라우팅된 전문가는 디스크(약 370GB)에 두었다가 라우팅 결정에 따라 필요한 것만 불러오는 "가중치용 JIT 컴파일러" 구조를 취합니다. 다음 레이어의 라우팅을 71.6% 정확도로 미리 예측해 프리페치하고, 듀얼 SSD 스트라이핑으로 읽기 대역폭을 두 배로 늘리는 등 스토리지 I/O 최적화 기법이 핵심입니다. 클라우드 없이 초거대 모델을 굴리려는 시도가 이제 순수 알고리즘이 아니라 스토리지·캐싱 시스템 설계의 문제로 옮겨가고 있음을 보여주는 사례입니다.

## vxcontrol/pentagi

- <https://github.com/vxcontrol/pentagi>
- 언어: Go
- 오늘 스타: 613 (총 23,752)

사람의 개입을 최소화하고 침투 테스트와 보안 진단을 자율적으로 수행하는 멀티 에이전트 시스템입니다. Go 백엔드와 React 프론트엔드 위에 Claude·GPT 등 여러 LLM 제공자를 연결하고, Docker 컨테이너와 GraphQL API로 에이전트 간 작업을 조율하는 구조입니다. 반복적인 정찰·취약점 탐색 작업을 에이전트에게 위임하려는 보안 업계의 수요와 LLM의 추론 능력 향상이 맞물려 꾸준히 관심을 받는 것으로 보이며, 오늘 함께 오른 위험 명령 차단·시스템 프롬프트 유출 프로젝트들과 겹쳐 보면 "에이전트를 보안 영역에서 얼마나 신뢰할 것인가"라는 질문이 공격과 방어 양쪽에서 동시에 진행되고 있음을 보여줍니다.

## calesthio/OpenMontage

- <https://github.com/calesthio/OpenMontage>
- 언어: Python
- 오늘 스타: 383 (총 58,180)

자연어 요청 하나로 리서치·대본·소재 생성·편집·최종 합성까지 영상 제작 전 과정을 코딩 에이전트가 지휘하도록 만든 오픈소스 시스템입니다. 도구·파이프라인(1계층), 마크다운으로 작성된 스킬(2계층), 외부 기술 문서(3계층)로 지식을 나누고, YAML 파이프라인 매뉴페스트를 읽어 단계별 디렉터 스킬을 따르며 7개 기준으로 점수를 매겨 20여 개 영상 생성 provider 중 하나를 선택하는 구조입니다. 렌더 전 검증과 렌더 후 자기 검토, 결정 근거를 남기는 감사 추적(audit trail)까지 갖춘 점에서, 단순히 "프롬프트 던지고 기대하는" 방식이 아니라 사람이 승인 지점을 통제할 수 있는 프로덕션급 에이전트 워크플로 설계를 참고하기 좋습니다.

## 오늘의 히든잼

### xs-lang0/xs

- <https://github.com/xs-lang0/xs>
- 언어: C
- 스타: 130 (생성일: 2026-03-06)

선택적 정적 타이핑, 패턴 매칭, 클로저, 트레이트를 갖춘 구조체를 지원하는 컴파일 언어로, 컴파일러·언어 서버·디버거·포매터·린터·테스트 러너·프로파일러·패키지 매니저가 단일 바이너리에 들어 있습니다. 바이트코드 VM, 트리워킹 인터프리터, x86-64/aarch64용 JIT, C/JavaScript 트랜스파일러까지 여러 백엔드를 갖춘 컴파일러 아키텍처를 직접 구현했고, 인터프리터와 VM 두 백엔드를 매 커밋마다 동일한 테스트로 diff해 결과가 갈리지 않는지 검증합니다. 적대적·정합성·퍼즈·골든·회귀 테스트까지 7개 계층으로 나눈 테스트 스위트와 언어 레퍼런스·구현 상태 문서를 함께 갖추고 있어, 개인 프로젝트치고는 언어 구현의 전 영역을 매우 진지하게 다루고 있다고 판단했습니다.

### aicheye/crustty

- <https://github.com/aicheye/crustty>
- 언어: Rust
- 스타: 54 (생성일: 2026-02-08)

C의 부분집합(정수, 문자, 구조체, 포인터, 배열, 제어 흐름)을 해석하면서 한 줄씩 되감아 볼 수 있는 타임트래블 디버거를 갖춘 교육용 C 인터프리터입니다. 직접 작성한 재귀 하강 파서에 이항 연산자용 precedence climbing을 적용했고, 스택 기반 실행과 콜 프레임, first-fit 힙 할당자를 구현한 뒤 문장 실행마다 스택·힙 전체 상태를 스냅샷으로 저장해 양방향 탐색이 가능하게 만들었습니다. 스택 변수와 힙 할당 영역을 겹치지 않는 주소 범위로 분리해 별도 타입 태그 없이도 UI가 포인터 종류를 구분하도록 한 메모리 모델 설계도 눈에 띕니다. `cargo test`로 실행되는 단위·통합 테스트와 아키텍처를 설명하는 README를 갖추고 있어, 인터프리터와 디버거 내부 구조를 함께 공부하기에 좋은 프로젝트입니다.

### TheUncharted/zapcode

- <https://github.com/TheUncharted/zapcode>
- 언어: Rust
- 스타: 90 (생성일: 2026-03-11)

AI 에이전트가 생성한 코드를 안전하게, 그것도 마이크로초 단위로 실행하기 위한 TypeScript 인터프리터입니다. oxc 파서로 파싱한 뒤 중간 표현을 거쳐 약 50개 명령으로 이루어진 스택 기반 바이트코드로 컴파일하고 VM에서 실행하는 파이프라인으로, Node.js나 V8 없이 약 2마이크로초 만에 시작합니다(Docker는 200~500ms, V8 isolate는 5~50ms). 파일시스템·네트워크·`eval`·`import`·프로토타입 오염을 기본 차단하는 deny-by-default 샌드박스와 메모리·실행 시간·스택 깊이·할당 횟수 제한을 언어 레벨에서 구현했고, VM 상태를 약 2KB로 스냅샷해 프로세스를 넘나들며 재개할 수 있는 기능도 갖췄습니다. 19개 공격 카테고리에 걸친 65개 적대적 테스트로 샌드박스 안전성을 검증하고 있어, 에이전트 실행 환경을 OS 격리가 아닌 언어 레벨에서 어떻게 안전하게 설계하는지 배우기 좋습니다.

### 0xff-ai/omnifs

- <https://github.com/0xff-ai/omnifs>
- 언어: Rust
- 스타: 87 (생성일: 2026-04-12)

GitHub, DNS, arXiv, Docker, Kubernetes 같은 외부 시스템을 로컬 디렉터리 구조로 투영해 `ls`, `cat`, `grep` 같은 표준 유닉스 도구로 다룰 수 있게 만드는 파일시스템 추상화 계층입니다. 리눅스에서는 FUSE, macOS에서는 읽기 전용 NFSv4로 호스트 네이티브 데몬을 노출하고, provider들은 `omnifs:provider`라는 표준 WIT 인터페이스를 구현한 샌드박스 Wasm 컴포넌트로 동작하며 자신의 신원·권한·인증 방식을 메타데이터로 선언합니다. 자격 증명은 호스트 데몬이 쥐고 있어 provider가 직접 네트워크나 크리덴셜에 접근할 수 없도록 경계를 두었고, 캐노니컬한 업스트림 페이로드를 여러 파일시스템 엔트리에 분산 캐싱하는 구조까지 갖췄습니다. Alpha 단계임을 명시하면서도 provider별 경로 레퍼런스와 개발 워크플로 문서를 충실히 제공하고 있어, FUSE·Wasm 컴포넌트·권한 경계를 결합한 시스템 설계를 살펴보기 좋은 프로젝트입니다.

### z1-0/nix-ast

- <https://github.com/z1-0/nix-ast>
- 언어: Haskell
- 스타: 84 (생성일: 2026-06-16)

Nix 표현식을 구조화된 AST로 파싱하고, 그 결과를 순수 Nix 함수로 변형·렌더링·평가할 수 있게 만드는 라이브러리입니다. 무거운 파싱은 Import From Derivation(IFD)을 통해 Haskell의 hnix 파서에 위임하고, AST 순회·재작성 같은 함수형 변형 로직은 외부 의존성 없이 순수 Nix로 구현해 두 언어의 강점을 나눠 쓰는 이중 계층 구조가 특징입니다. 18가지 표현식 생성자와 문자열 타입, 바인딩 변형까지 AST 전체를 세밀하게 다루고, 생성자마다 런타임 유효성 검사와 명확한 오류 메시지를 붙여 타입 안전성을 신경 쓴 흔적이 보입니다. 모든 노드 타입을 필드·JSON 표현·예제와 함께 정리한 전용 AST 레퍼런스 문서를 갖추고 있어, 설정 언어를 위한 파서-AST 라이브러리를 어떻게 문서화해야 하는지도 함께 참고할 만합니다.
