---
title: "GitHub Trending 2026-07-18"
tags: [github-trending]
---

오늘 트렌딩 1위는 스트리밍 비디오에서 3D 장면을 실시간으로 복원하는 파운데이션 모델(lingbot-map)이 차지하며 컴퓨터 비전 쪽 화제가 두드러졌습니다. 그 옆으로는 여러 BI·분석 도구 사이에 흩어진 의미 메타데이터를 표준화하려는 Apache 프로젝트(ossie)와, 매달 수백만 이벤트를 처리하는 성숙한 분석 플랫폼(PostHog)이 나란히 자리를 지키고 있어 "데이터를 어떻게 정의하고 관측할 것인가"라는 주제가 꾸준히 이어지는 인상입니다. 여기에 디자인 엔지니어를 위한 UI 스킬 모음(ui-skills)과 503개 레슨짜리 AI 엔지니어링 커리큘럼(ai-engineering-from-scratch)까지 겹치면서, 실무 도구화와 밑바닥부터 배우는 학습 콘텐츠가 함께 상위권에 오른 하루입니다.

## Robbyant/lingbot-map

- <https://github.com/Robbyant/lingbot-map>
- 언어: Python
- 오늘 스타: 827 (총 12,577)

스트리밍 비디오로부터 3D 장면을 실시간 복원하는 feed-forward 파운데이션 모델입니다. "Geometric Context Transformer"라는 아키텍처로 좌표 그라운딩, 조밀한 기하 단서, 장거리 드리프트 보정을 하나의 스트리밍 프레임워크 안에 통합하고, paged KV 캐시 어텐션으로 518×378 해상도에서 1만 프레임이 넘는 영상도 초당 약 20프레임으로 처리한다고 밝히고 있습니다. 최적화 기반 방식 없이 한 번의 순전파로 장면을 복원한다는 점과, 앵커 컨텍스트·포즈 참조 윈도우·궤적 메모리로 긴 시퀀스에서도 안정성을 유지하는 설계는 실시간 3D 재구성 모델을 공부할 때 눈여겨볼 부분입니다.

## apache/ossie

- <https://github.com/apache/ossie>
- 언어: Python
- 오늘 스타: 48 (총 1,197)

analytics·AI·BI 플랫폼마다 제각각 정의되는 동일한 비즈니스 지표를 하나의 벤더 중립적인 시맨틱 모델 표준으로 묶으려는 Apache 프로젝트로, 최근 며칠 트렌딩에 꾸준히 오르고 있습니다. JSON/YAML 기반 핵심 스펙에 dbt·GoodData·Polaris·Salesforce 등으로의 변환기와 검증 도구, TPC-DS 예제 모델까지 갖추고 있고, 이슈 19건·PR 36건이 열려 있는 등 커뮤니티가 활발히 스펙을 다듬고 있습니다. 여러 도구 사이의 "의미 파편화" 문제를 스키마와 컨버터로 구조적으로 해결하려는 접근은, 표준화 프로젝트가 어떻게 레퍼런스 구현으로 신뢰를 쌓는지 참고하기 좋습니다.

## PostHog/posthog

- <https://github.com/PostHog/posthog>
- 언어: Python
- 오늘 스타: 337 (총 36,427)

제품 분석, 세션 리플레이, 피처 플래그, 에러 추적, LLM 애플리케이션을 위한 AI observability까지 한 플랫폼에 모은 오픈소스 분석 도구로, 최근 스스로를 "self-driving products를 만드는 플랫폼"으로 포지셔닝하며 다시 상위권에 올랐습니다. Python·TypeScript·Rust로 이뤄진 코드베이스에 5만 건이 넘는 커밋이 쌓여 있고, 오픈소스 코어와 `ee` 디렉터리로 분리된 프로프라이어터리 기능을 함께 두는 구조가 특징입니다. 여러 관측 기능(분석, 리플레이, 에러 추적)을 하나의 이벤트 파이프라인 위에 올려 확장한 아키텍처는, 단일 제품이 관측 가능성 스위트로 성장하는 과정을 공부하기 좋은 사례입니다.

## ibelick/ui-skills

- <https://github.com/ibelick/ui-skills>
- 언어: TypeScript
- 오늘 스타: 529 (총 4,787)

디자인 엔지니어를 위한 UI 패턴을 스킬 단위로 큐레이션해 `npx ui-skills get baseline-ui`처럼 CLI로 바로 가져다 쓸 수 있게 만든 저장소입니다. TypeScript와 Astro로 구현된 웹사이트(ui-skills.com)와 CLI가 같은 스킬 카탈로그를 공유하는 구조로, 카테고리별 필터링과 검색을 지원합니다. 코딩 에이전트에게 절차적 지식을 넘기는 "스킬 파일" 흐름이 최근 며칠 트렌딩에 반복해서 등장했는데, 이 프로젝트는 그 대상을 UI 구현 패턴으로 좁혀 실무 재사용성을 높인 사례로 볼 수 있습니다.

## rohitg00/ai-engineering-from-scratch

- <https://github.com/rohitg00/ai-engineering-from-scratch>
- 언어: Python
- 오늘 스타: 232 (총 38,936)

"AI를 배우는 게 아니라 손으로 직접 만든다"는 모토로, 20개 phase에 걸쳐 503개 레슨(약 320시간 분량)을 Python·TypeScript·Rust·Julia로 구성한 커리큘럼입니다. 각 레슨이 Motto-Problem-Concept-Build It-Use It-Ship It이라는 동일한 6단계 패턴을 따르고, 수학적 기초부터 직접 알고리즘을 구현한 뒤에야 PyTorch 같은 프레임워크로 넘어가는 순서를 지킨다는 점이 특징입니다. 딥러닝·NLP·트랜스포머를 거쳐 에이전트 엔지니어링과 멀티에이전트 협업까지 다루는 범위와, 레슨마다 재사용 가능한 산출물(프롬프트, 스킬, MCP 서버)을 남기도록 설계한 구조는 "밑바닥부터 배우기" 콘텐츠를 어떻게 끝까지 완주 가능한 형태로 쪼개는지 참고할 만합니다.

## 오늘의 히든잼

### samyeyo/clx

- <https://github.com/samyeyo/clx>
- 언어: C++ · 스타 120 · 생성일 2026-06-14

Lua 스크립트를 바이트코드 인터프리터 대신 C++ 소스로 변환해 네이티브 실행 파일로 컴파일하는 AOT Lua 컴파일러입니다. 8바이트 페이로드에 별도 타입 태그를 붙인 16바이트 태그드 값 표현, 짧은 문자열 인라인 최적화, 테이블 접근 패스트 패스 캐시 같은 구체적인 최적화 기법을 README에서 직접 설명하고 있고, Lua 5.5의 코루틴·메타테이블까지 목표 호환 범위로 명시합니다. 모든 Lua 예제 파일을 실제로 컴파일·실행해 `[OK]`/`[FAIL]`로 검증하는 테스트 스위트와, Lua 5.5·LuaJIT 대비 구체적인 하드웨어·컴파일러 조건을 밝힌 벤치마크, Pong 게임·만델브로트 뷰어 같은 동작하는 예제까지 갖추고 있어 AOT 컴파일러 설계를 실물로 살펴보기 좋은 프로젝트입니다.

### OrlovEvgeny/serde.zig

- <https://github.com/OrlovEvgeny/serde.zig>
- 언어: Zig · 스타 69 · 생성일 2026-03-06

JSON, MessagePack, TOML, YAML, XML 등 여러 포맷을 매크로나 코드 생성 없이 Zig의 `@typeInfo` comptime 리플렉션만으로 직렬화·역직렬화하는 라이브러리입니다. 포맷마다 `toSlice`/`fromSlice`/`toWriter`/`fromReader`로 통일된 인터페이스를 두고, 이스케이프가 없는 JSON은 입력 버퍼를 그대로 가리키는 슬라이스를 반환하는 제로카피 방식까지 구현해뒀습니다. Zig 0.15부터 0.17-dev까지 세 버전을 대상으로 한 CI, `zig build test`로 도는 테스트 스위트, 포맷별 성능을 비교하는 벤치마크, RFC 8259 준수를 위한 호환성 파괴적 변경 사항을 명시적으로 문서화한 마이그레이션 가이드까지 갖추고 있어 comptime 기반 직렬화 프레임워크의 설계를 공부하기 좋습니다.

### timur-developer/gcscope

- <https://github.com/timur-developer/gcscope>
- 언어: Go · 스타 33 · 생성일 2026-05-23

Go 런타임의 가비지 컬렉터 동작을 실시간으로 들여다보는 터미널 UI 도구입니다. `GODEBUG=gctrace=1`로 실행한 바이너리의 stderr를 파싱하는 run 모드와, `runtime/metrics`를 노출하는 HTTP 엔드포인트를 폴링하는 attach 모드 두 가지로 GC 사이클, stop-the-world 정지 시간, 힙 증감, 페이서 신호를 수집합니다. 기본 200개 샘플의 슬라이딩 윈도우로 확대·축소가 가능한 실시간 차트를 그리고 스냅샷 비교 기능까지 제공하며, GitHub Actions CI와 `go test ./...` 기반 테스트, CLI·reporter 패키지·UI를 분리한 구조, 러시아어 번역까지 포함한 문서가 갖춰져 있어 런타임 관측 도구를 직접 만들 때 참고할 만한 완성도입니다.

### webc-site/math

- <https://github.com/webc-site/math>
- 언어: JavaScript · 스타 126 · 생성일 2026-06-06

LaTeX 수식을 빌드 타임에 브라우저 네이티브 MathML로 컴파일해, MathJax나 KaTeX 같은 무거운 런타임 라이브러리 없이도 수식을 렌더링할 수 있게 하는 컴파일러입니다. 마크다운에서 수식 구분자를 찾는 스캐너, LaTeX 토큰화 렉서, AST를 만드는 파서, MathML 태그로 변환하는 코드젠까지 교과서적인 멀티스테이지 컴파일러 파이프라인을 그대로 구현했고, 초당 30만 연산 이상(KaTeX 대비 3배, MathJax 대비 40배 빠르다고 주장)이라는 구체적인 성능 수치와 7.69KB(gzip 3.56KB)의 의존성 없는 코어 크기를 함께 제시합니다. `test.sh` 테스트 러너와 oxlint·oxfmt 같은 전문적인 린트·포맷 도구 체인, markdown-it·marked·remark 통합, 영어·중국어 이중 문서까지 갖추고 있어 작은 규모의 컴파일러를 처음부터 끝까지 설계하는 법을 살펴보기 좋습니다.
