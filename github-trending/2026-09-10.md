---
title: "GitHub Trending 2026-09-10"
tags: [github-trending]
---

오늘 트렌딩은 코딩 에이전트를 둘러싼 "제어" 문제가 여러 각도에서 동시에 나타난 하루입니다. 에이전트의 응답 스타일 자체를 규율하는 스킬(i-have-adhd), 에이전트가 설계·계획·TDD 절차를 강제로 따르게 만드는 방법론(superpowers), 팀 전체의 스킬·규칙을 한 저장소로 동기화하는 배포 도구(teamai-cli)가 나란히 상위권에 올라, "에이전트가 무엇을 어떻게 하게 할 것인가"라는 질문이 응답 포맷부터 조직 운영까지 층층이 파고들고 있음을 보여줍니다. 그 옆에서 실제 자산을 굴리는 자율 트레이딩 에이전트(CloddsBot)가 다시 등장했고, 흩어진 실시간 지리공간 데이터를 하나의 3D 지구본으로 통합한 시각화 프로젝트(gods-eye-view)가 에이전트 생태계 밖에서도 눈길을 끕니다.

## ayghri/i-have-adhd

- <https://github.com/ayghri/i-have-adhd>
- 언어: Python
- 오늘 스타: 3,854 (총 37.1k)

AI 응답이 장황한 서두와 맥락 설명으로 정작 필요한 행동 지침을 파묻어버리는 문제를 겨냥한 Claude 스킬입니다. "다음 행동부터 제시하라", "여러 단계 작업은 번호를 매겨라", "서두·요약·맺음말 없이 끝내라" 같은 10가지 규칙으로 응답 포맷을 재작성하며, `npm install jsonwebtoken@latest` 후 `src/auth.ts:42`를 고치라는 식의 즉시 실행 가능한 지시를 만들어냅니다. `SKILL.md` 하나로 모델의 출력 스타일 전체를 바꿀 수 있다는 점에서, 프롬프트가 아니라 스킬 형태로 "대화 습관"을 패키징하는 최근 흐름을 잘 보여주는 사례입니다.

## bilawalsidhu/gods-eye-view

- <https://github.com/bilawalsidhu/gods-eye-view>
- 언어: JavaScript
- 오늘 스타: 1,588 (총 22.8k)

항공기·선박·위성·지진·CCTV처럼 따로 흩어져 있던 공개 지리공간 데이터를 CesiumJS 기반의 포토리얼리스틱 3D 지구본 하나로 통합한 프로젝트입니다. OpenSky·AISStream·CelesTrak·USGS 같은 공개 API를 모아 15~30초 주기로 갱신하고, SGP4 궤도 전파와 데드 레커닝으로 그 사이를 보간해 실시간처럼 보이게 만드는 방식이 흥미롭습니다. OpenAI Realtime API로 음성 명령을 받아 카메라 이동이나 주석 같은 28개 도구를 실행하는 음성 에이전트까지 얹혀 있어, 여러 실시간 데이터 소스를 하나의 일관된 시각화 레이어로 합치는 아키텍처를 공부하기 좋습니다.

## obra/superpowers

- <https://github.com/obra/superpowers>
- 언어: Shell
- 오늘 스타: 731 (총 284.5k)

코딩 에이전트가 곧바로 구현에 뛰어드는 대신 설계 질문 → 승인 → 잘게 쪼갠 계획 → RED-GREEN-REFACTOR 방식의 TDD를 강제로 거치도록 만드는 스킬·워크플로 프레임워크로, 오랫동안 트렌딩 상위권을 지키고 있습니다. 핵심은 "subagent-driven-development"로, 개별 작업을 새 서브에이전트가 맡아 스펙 준수와 코드 품질을 두 단계로 검토받게 하는 구조입니다. Claude Code·Cursor·Copilot CLI 등 여러 에이전트에 이식 가능한 플러그인 아키텍처로 되어 있어, "선택 가능한 제안"이 아니라 "자동으로 발동하는 의무 절차"로 엔지니어링 규율을 강제하는 설계를 참고할 만합니다.

## alsk1992/CloddsBot

- <https://github.com/alsk1992/CloddsBot>
- 언어: TypeScript
- 오늘 스타: 299 (총 1.4k)

Polymarket·Kalshi·Binance·Hyperliquid 등 16개 이상의 거래소·프로토콜에 걸쳐 자율적으로 매매하는 트레이딩 에이전트로, Claude 기반 4개 전문 에이전트(Main·Trading·Research·Alerts)가 119개 스킬과 18개 도구를 나눠 쓰는 구조입니다. 서킷 브레이커·VaR 계산·켈리 기준 포지션 사이징 같은 리스크 관리 엔진과, 모든 거래 판단의 근거를 남기는 감사 로그, 실전 배포 전 백테스트 검증까지 갖추고 있다는 점이 눈에 띕니다. 이전 다이제스트의 Vibe-Trading과 마찬가지로, 손실이 실제로 발생하는 도메인에 자율 에이전트를 투입할 때 어떤 안전장치 계층을 쌓는지 비교해서 보기 좋은 사례입니다.

## Tencent/teamai-cli

- <https://github.com/Tencent/teamai-cli>
- 언어: TypeScript
- 오늘 스타: 837 (총 3.6k)

팀원마다 Claude Code, Cursor, CodeBuddy 등 서로 다른 AI 에이전트를 쓰더라도 스킬·규칙·MCP 설정·지식을 한 Git 저장소로 동기화하는 CLI 도구입니다. `teamai push`로 브랜치·머지 리퀘스트를 만들고 리뷰·머지 후 `teamai pull`로 각자의 로컬 설정에 자동 반영하는 구조인데, 세션 중 사용자가 AI 응답을 수정·중단한 지점을 자동으로 감지해 지식 베이스를 쌓는 "friction detection"과, tree-sitter 기반 AST 파싱으로 코드 구조를 그래프화하는 BM25 검색까지 포함하고 있습니다. 개인용 에이전트 설정을 넘어 "팀 전체의 에이전트 표준"을 코드처럼 버전 관리하는 접근을 볼 수 있는 프로젝트입니다.

## 오늘의 히든잼

### wkoszek/emuko

- <https://github.com/wkoszek/emuko>
- 언어: Rust · 스타: 103 · 생성일: 2026-02-16

RV64IMAFDC 명령어 집합과 M/S/U 권한 레벨, Sv39 가상 메모리를 구현해 실제로 Debian Linux를 부팅시키는 RISC-V 에뮬레이터입니다. ARM64·x86_64 호스트에서는 JIT로, 그 외에는 인터프리터로 폴백하는 하이브리드 실행 구조를 쓰는데, README에 "인터프리터는 QEMU보다 3배, JIT는 아직 코드 블록 체이닝이 없어 29배 느리다"고 성능 한계를 스스로 명시해둔 점이 신뢰가 갑니다. UART 16550·CLINT·PLIC·SBI 1.0 같은 주변장치 구현과 전체 머신 상태 스냅샷 저장/복원 기능까지 갖추고 있어, CPU 에뮬레이션과 가상 메모리를 밑바닥부터 구현하는 법을 공부하기 좋은 프로젝트입니다.

### multikernel/daxfs

- <https://github.com/multikernel/daxfs>
- 언어: C · 스타: 100 · 생성일: 2026-01-24

CXL 메모리나 영속 메모리처럼 여러 커널·호스트가 공유하는 DAX 메모리 위에서 동작하는 파일시스템으로, 중앙 조정자 없이 하드웨어 CAS(compare-and-swap) 원자 연산만으로 락 없는 동시 쓰기를 구현한 것이 핵심입니다. 정적 베이스 이미지, 쓰기 가능한 메타데이터 오버레이, 완전 쓰기 가능 모드 3가지로 동작하며, 커널 모듈과 mkdaxfs·daxfs-inspect 같은 도구, 슈퍼블록·아이노드 테이블 구조를 상세히 규정한 v7 포맷 스펙까지 갖춰 스텁 수준을 넘어선 완성도를 보입니다. 여러 호스트가 캐시 일관성 도메인 안에서 포인터 없이 검증 가능한 메모리 구조를 공유하는 디스크 없는 파일시스템 설계를 살펴보기 좋습니다.

### Raminshiraz/raft-visualizer

- <https://github.com/Raminshiraz/raft-visualizer>
- 언어: JavaScript · 스타: 125 · 생성일: 2026-07-28

Raft 합의 알고리즘을 논문 Figure 2 그대로 구현한 순수 JS 엔진 위에 React UI를 얹은 인터랙티브 시각화 도구로, 노드를 죽이거나 네트워크를 단방향으로만 끊는 등 다른 시각화 도구들이 잘 다루지 않는 병리적 시나리오(Figure 8 케이스, 비대칭 단절 등)까지 재현합니다. 특히 헤드리스로 `app.js` 엔진을 50개의 무작위 시드로 반복 실행하며 매 40ms 틱마다 선출 안전성·로그 일치성·상태 머신 안전성·리더 완전성 4대 안전 속성을 기계적으로 검증하는 자동 테스트가 인상적입니다. Raft 위에 2단계 커밋(2PC)을 얹어 여러 클러스터의 트랜잭션 원자성까지 시뮬레이션하는 부분도 있어, "눈으로 보는" 수준을 넘어 검증 가능한 합의 알고리즘 구현체로서 배울 점이 많습니다.

### drmingdrmer/ezraft

- <https://github.com/drmingdrmer/ezraft>
- 언어: Rust · 스타: 43 · 생성일: 2026-08-01

저수준 Raft 구현체인 openraft 위에서 선출·복제·멤버십 변경·스냅샷·네트워킹을 대신 처리해주는 얇은 프레임워크로, 사용자는 애플리케이션 상태와 요청 처리를 정의하는 `EzApp`, 디스크 영속화를 담당하는 `EzStorage` 두 트레이트만 구현하면 장애 허용 복제 클러스터를 얻을 수 있습니다. 내장 HTTP API와 파일 기반 `FileStorage` 구현체, 동작하는 키-값 저장소 예제와 API 문서·CHANGELOG까지 갖추고 있어 "실험적" 딱지에도 불구하고 실사용을 염두에 둔 완성도를 보여줍니다. openraft의 복잡한 설정을 두 인터페이스로 추상화한 설계는, 저수준 합의 라이브러리를 실제 애플리케이션에 통합할 때 어떤 경계에서 복잡도를 감출지 고민할 때 참고할 만합니다.
