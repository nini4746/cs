---
title: "GitHub Trending 2026-07-24"
tags: [github-trending]
---

오늘 트렌딩은 "사람과 에이전트가 같은 작업 공간을 어떻게 공유할 것인가"라는 주제와 "핵심 소프트웨어를 Rust로 다시 짠다"는 흐름이 나란히 눈에 띄는 하루입니다. 사람·에이전트·git 이벤트를 하나의 relay로 묶으려는 협업 플랫폼(buzz)과 78개 이상의 SaaS 연동을 담은 Claude Skills 모음집(awesome-claude-skills)이 상위권에 올랐고, 그 옆으로는 AI 뉴스 대시보드(worldmonitor), 금융 캔들 예측 파운데이션 모델(Kronos), Rust로 다시 만든 마인크래프트 서버(Pumpkin)까지 성격이 다른 프로젝트들이 섞여 있습니다. 오늘의 히든잼에서는 트렌딩과는 결이 다르게, RISC-V 에뮬레이터·AOT Lua 컴파일러·바닥부터 만든 OS·데이터 엔지니어링 시스템 재구현 모음처럼 "밑바닥부터 이해하기" 좋은 자료들을 모아봤습니다.

## block/buzz

- <https://github.com/block/buzz>
- 언어: Rust
- 오늘 스타: 3,274 (총 8,915)

Block(舊 Square)이 만든 셀프호스팅 워크스페이스로, Nostr relay를 기반 삼아 사람과 AI 에이전트가 같은 이벤트 로그 위에서 프로젝트·코드리뷰·워크플로·인시던트 대응을 함께 처리하도록 설계되어 있습니다. `buzz-relay`(Axum WebSocket/REST), `buzz-db`(PostgreSQL), `buzz-pubsub`(Redis), `buzz-media`(S3/MinIO, Blossom 프로토콜), 그리고 Goose·Codex·Claude Code 같은 에이전트를 붙이는 `buzz-acp` 하네스까지 여러 Rust crate로 나뉘어 있고, 에이전트마다 별도의 암호학적 identity와 감사 로그를 갖는 점이 특징입니다. 에이전트를 "봇"이 아니라 자체 신원을 가진 참여자로 취급하는 협업 인프라 설계를 구체적으로 살펴보기 좋은 프로젝트입니다.

## koala73/worldmonitor

- <https://github.com/koala73/worldmonitor>
- 언어: TypeScript
- 오늘 스타: 2,194 (총 72,901)

500개 이상의 뉴스 피드를 AI로 요약해 지정학·인프라 상황을 실시간으로 보여주는 대시보드입니다. 3D 글로브(globe.gl+Three.js)와 WebGL 평면 지도(deck.gl+MapLibre) 두 가지 매핑 엔진, 60개 이상의 Vercel Edge Function과 Protocol Buffers 기반 백엔드, 그리고 API 키 없이 로컬에서 돌아가는 Ollama 추론까지 갖추고 있어 하나의 코드베이스에서 world/tech/finance 등 6개 사이트 변형을 뽑아내는 구조입니다. 대용량 실시간 데이터를 지도·타임라인 UI로 녹여내면서도 로컬 LLM 추론 경로를 기본값으로 설계한 부분이, 서비스형 AI 대시보드를 만들 때 참고할 만합니다.

## ComposioHQ/awesome-claude-skills

- <https://github.com/ComposioHQ/awesome-claude-skills>
- 언어: Markdown 중심
- 오늘 스타: 662 (총 69,851)

Claude.ai, Claude Code, Cursor, Gemini CLI 등에서 쓸 수 있는 1,000개 이상의 Claude Skills·플러그인을 문서 처리, 개발 도구, 데이터 분석, 협업 등 카테고리별로 정리한 큐레이션 저장소입니다. 각 스킬은 YAML frontmatter와 Markdown 지시문으로 이루어진 공개 포맷을 따르고, "SKILL.md 본문은 보통 5,000토큰 이하이며 에이전트가 관련 있다고 판단할 때만 로드된다"는 설계 원칙을 명시하고 있습니다. 스킬이라는 단위로 에이전트의 절차적 지식을 어떻게 모듈화·공유하는지 한눈에 훑어보기 좋은 색인입니다.

## Pumpkin-MC/Pumpkin

- <https://github.com/Pumpkin-MC/Pumpkin>
- 언어: Rust
- 오늘 스타: 472 (총 9,211)

Java/Bedrock 에디션 마인크래프트 서버를 처음부터 Rust로 다시 구현하는 프로젝트로, 프로토콜 처리·월드 관리·인벤토리·NBT 직렬화·플러그인 API를 각각 독립된 crate로 나누고 멀티스레딩으로 성능을 끌어올리는 것을 목표로 합니다. 서버 상태/핑, 암호화, 패킷 압축, 청크 로딩 포맷, BungeeCord/Velocity 프록시 지원까지 vanilla 서버의 세부 동작을 하나씩 재현하고 있어, 실제 네트워크 게임 서버의 프로토콜·월드 상태 관리 구조를 공부하기에 좋은 예제입니다.

## shiyu-coder/Kronos

- <https://github.com/shiyu-coder/Kronos>
- 언어: Python
- 오늘 스타: 506 (총 33,379)

45개 이상의 글로벌 거래소 데이터로 학습한, 금융 캔들(K-line) 예측을 위한 오픈소스 파운데이션 모델입니다. OHLCV 연속값을 계층적 이산 토큰으로 양자화하는 전용 tokenizer와 이를 학습하는 디코더 전용 autoregressive Transformer, 이렇게 2단계 구조로 되어 있고 4.1M~499.2M 파라미터의 mini/small/base/large 모델 zoo와 커스텀 데이터셋 파인튜닝 파이프라인(Qlib 연동)을 제공합니다. 시계열 데이터를 "토큰화 가능한 언어"로 바라보고 범용 언어모델 아키텍처를 그대로 가져다 쓴 점에서, 도메인 특화 foundation model을 설계하는 한 가지 방식을 볼 수 있습니다.

## 오늘의 히든잼

### wkoszek/emuko

- <https://github.com/wkoszek/emuko>
- 언어: Rust · 스타 103 · 생성일 2026-02-16

RV64IMAFDC 명령어 집합과 M/S/U 권한 레벨, Sv39 가상 메모리까지 구현해 실제로 Linux 커널을 부팅시키는 RISC-V 에뮬레이터입니다. ARM64/x86_64 호스트에서는 JIT로, 지원되지 않는 경우 인터프리터로 자동 전환하는 하이브리드 실행 구조를 갖추고 있고, README에 인터프리터는 QEMU 대비 약 3배, JIT는 약 29배 느리다는 성능 수치와 코드 블록 체이닝이 아직 미구현이라는 한계까지 솔직하게 적어뒀습니다. JIT와 인터프리터의 실행 결과를 서로 비교하는 differential checker까지 갖추고 있어, CPU 에뮬레이터를 처음부터 설계할 때 정확성을 어떻게 검증하는지 함께 배우기 좋습니다.

### samyeyo/clx

- <https://github.com/samyeyo/clx>
- 언어: C++ · 스타 135 · 생성일 2026-06-14

Lua 5.5 프로그램을 바이트코드 인터프리터가 아니라 C++20 백엔드를 거쳐 실제 네이티브 실행 파일로 컴파일하는 AOT(ahead-of-time) 컴파일러입니다. 8바이트 payload와 별도 타입 태그로 이루어진 16바이트 tagged value 표현, 6바이트 이하 문자열을 값 안에 바로 inline하는 최적화, 테이블 접근을 위한 fast-path 캐시 등 인터프리터 언어를 네이티브 코드로 내리는 과정의 구체적인 기법들을 담고 있습니다. `tests/` 아래 각 Lua 파일을 컴파일·실행·assertion 검증하는 자동화 테스트 스위트와 CLI·호환성·런타임 내부 구조를 각각 다루는 `doc/` 디렉터리를 갖추고 있어, 언어 구현체를 벤치마크(fibonacci 기준 62.20배 속도 향상 보고)와 함께 문서화하는 방식을 참고할 만합니다.

### BerkeOruc/BerkeOS

- <https://github.com/BerkeOruc/BerkeOS>
- 언어: Rust · 스타 39 · 생성일 2026-03-19

DOS 감성의 x86_64 운영체제를 `no_std` Rust로 바닥부터 만든 프로젝트로, NASM 부트로더가 32비트에서 64비트 Long Mode로 전환한 뒤 커스텀 빌드 파이프라인(NASM→Cargo→링커→GRUB ISO)으로 이어지는 전체 부팅 체인을 직접 구현했습니다. 2MiB huge page 기반 메모리·페이징, PIC/PIT 기반 인터럽트, ATA PIO 드라이버 위에 만든 자체 파일시스템 BerkeFS, 30개 이상 명령을 지원하는 셸(berkesh)까지 갖추고 있고, fsck 유틸리티와 QEMU 기반 CI로 검증하고 있습니다. 약 1만 4천 줄 규모의 코드에 대해 부트 흐름을 Mermaid 다이어그램으로 문서화해둔 점도 눈에 띄어서, OS 커널을 처음 만들어보려는 사람이 각 서브시스템을 어떤 순서로 쌓아가는지 참고하기 좋습니다.

### sophie-nguyenthuthuy/data-engineering

- <https://github.com/sophie-nguyenthuthuy/data-engineering>
- 언어: Python · 스타 51 · 생성일 2026-05-12

Kafka, Iceberg, Delta Lake, Hudi 같은 실제 데이터 플랫폼의 알고리즘과 아키텍처를 최소 의존성 Python 코드로 밑바닥부터 재구현한 159개짜리 모노레포입니다. 스트리밍/실시간, 배치/ETL, 데이터 품질·거버넌스, 쿼리·스토리지 엔진, 웨어하우스/레이크하우스, 분산 시스템 6개 카테고리에 걸친 79개의 프로덕션급 구현과, 쿼리 컴파일·CRDT·정형 검증(TLA+) 같은 주제를 다루는 21개의 리서치 MVP로 나뉘어 있습니다. 프로젝트마다 독립적인 README·테스트·Docker 설정·CI를 갖추고 있고 전체 2,500개 이상의 테스트를 `mypy --strict`로 타입 검사까지 통과시킨다고 밝히고 있어, MySQL binlog 파서나 LSM 트리, 비잔틴 장애 허용 합의 같은 내부 동작을 직접 코드로 따라가며 공부하기에 좋은 자료입니다.
