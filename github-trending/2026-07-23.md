---
title: "GitHub Trending 2026-07-23"
tags: [github-trending]
---

오늘 트렌딩은 "에이전트가 일할 공간을 어떻게 마련해줄 것인가"라는 질문이 여러 형태로 반복됩니다. Nostr 릴레이 위에 사람과 에이전트가 함께 들어가는 협업 공간(buzz), 에이전트 전용 브라우저 탭을 분리해주는 macOS 브라우저(ego-lite), 그리고 에이전트가 실제 작업을 대행하도록 스킬을 표준화한 카탈로그(awesome-claude-skills)까지, 에이전트에게 "자기만의 작업 환경"을 내주는 흐름이 뚜렷합니다. 그 옆에서 금융 시계열 예측 파운데이션 모델(Kronos)과 Rust로 새로 짜는 Minecraft 서버(Pumpkin)가 각자의 도메인에서 성능과 정확도를 밀어붙이는 프로젝트로 함께 올라와 있습니다.

## block/buzz

- <https://github.com/block/buzz>
- 언어: Rust
- 오늘 스타: 3,252 (총 5,742)

사람과 AI 에이전트가 같은 룸에서 협업하는 셀프 호스팅 워크스페이스로, Nostr 릴레이를 기반으로 모든 메시지·코드 패치·워크플로 단계·승인이 서명된 이벤트로 감사 로그에 남습니다. Rust로 작성된 릴레이(Axum WebSocket + REST)가 PostgreSQL·Redis·S3/MinIO를 백엔드로 쓰고, 에이전트는 봇이 아니라 자기 키와 권한을 가진 팀원으로 취급됩니다. 채팅·코드 리뷰·CI·버전 관리·문서를 하나의 이벤트 로그로 통합하면서도 암호학적 감사 추적과 범위 지정 접근 제어를 유지하는 구조라, 에이전트에게 "사람과 동일한 협업 표면"을 어떻게 안전하게 내줄지 설계할 때 참고할 만합니다.

## shiyu-coder/Kronos

- <https://github.com/shiyu-coder/Kronos>
- 언어: Python
- 오늘 스타: 398 (총 32,888)

45개 이상의 글로벌 거래소 K-라인(캔들스틱) 데이터를 학습해 미래 OHLCV 값을 예측하는 금융 시장 전용 파운데이션 모델입니다. 연속적인 다차원 K-라인 데이터를 계층적 이산 토큰으로 바꾸는 전용 토크나이저와, 그 위에서 학습하는 디코더 전용 오토리그레시브 트랜스포머로 구성된 2단계 구조를 취하고, 410만~1억 230만 파라미터 규모의 세 가지 모델과 최대 2,048 토큰 컨텍스트를 제공합니다. 일반 시계열 모델이 아니라 금융 데이터의 노이즈 특성에 맞춰 설계됐고 AAAI 2026에 채택됐다는 점에서, 도메인 특화 파운데이션 모델을 처음부터 어떻게 설계하는지 살펴보기 좋은 사례입니다.

## Pumpkin-MC/Pumpkin

- <https://github.com/Pumpkin-MC/Pumpkin>
- 언어: Rust
- 오늘 스타: 563 (총 8,742)

바닐라 게임 메커닉을 유지하면서 Java·Bedrock 에디션을 모두 지원하는 Rust 기반 Minecraft 서버 구현체입니다. 멀티스레드 구조로 성능을 최적화했고, RCON·Query·채팅·권한·번역 같은 서버 기능과 Bungeecord·Velocity 같은 프록시 연동, 청크 생성/저장, 패킷 압축을 포함한 플레이어 인증까지 직접 구현했습니다. 복잡한 게임 서버를 시스템 프로그래밍 언어로 통째로 재작성하는 대형 프로젝트라, 네트워크 프로토콜·월드 상태·엔티티 관리를 서브시스템 단위로 어떻게 쪼개 관리하는지 로드맵을 통해 관찰할 수 있습니다.

## citrolabs/ego-lite

- <https://github.com/citrolabs/ego-lite>
- 언어: JavaScript
- 오늘 스타: 219 (총 1,377)

사람과 AI 에이전트가 같은 브라우저에서 서로 방해받지 않고 동시에 작업하도록 만든 macOS 브라우저로, 에이전트는 별도 "Space" 안에서 격리되어 동작하고 사람은 평소처럼 브라우징합니다. 커널 레벨 커스터마이징으로 웹페이지 스냅샷을 뜨고, 에이전트는 CLI 명령이 아니라 `ego-browser` 스킬의 JavaScript 함수로 상호작용하며, Chrome의 로그인·쿠키·확장 프로그램을 그대로 승계합니다. 탭·로그인 충돌 없이 여러 워크스페이스에서 병렬 작업이 가능하다는 점에서, 에이전트 전용 실행 환경을 브라우저 레벨에서 어떻게 분리 설계하는지 참고할 만합니다.

## ComposioHQ/awesome-claude-skills

- <https://github.com/ComposioHQ/awesome-claude-skills>
- 언어: Python
- 오늘 스타: 637 (총 69,213)

문서 처리·개발 도구·데이터 분석·비즈니스 자동화·500개 이상의 외부 앱 연동까지 아우르는 1,000개 이상의 프로덕션급 Claude Skill을 정리한 카탈로그입니다. 스킬 메타데이터(약 100토큰)만 먼저 로드하고 전체 내용(약 5,000토큰)은 필요할 때만 불러오는 점진적 로딩 구조를 취해 컨텍스트를 아끼며, Claude.ai·Claude Code·Claude API뿐 아니라 Cursor·Codex·Gemini CLI 등 여러 플랫폼에서 쓸 수 있는 오픈 표준을 따릅니다. 절차적 지식을 "필요할 때만 불러오는" 형태로 패키징하는 방법과, 그 위에 Composio MCP 서버로 실제 액션(이메일 발송, 이슈 생성 등)까지 연결하는 계층 구조를 함께 살펴볼 수 있습니다.

## 오늘의 히든잼

### 0xff-ai/omnifs

- <https://github.com/0xff-ai/omnifs>
- 언어: Rust · 스타: 86 · 생성일: 2026-04-12

GitHub, DNS, Docker, arXiv, Linear, SQLite, Kubernetes 같은 외부 시스템을 로컬 파일시스템 경로로 투영해, grep·find·cat·jq 같은 표준 유닉스 도구로 그대로 탐색할 수 있게 해주는 프로젝트입니다. Rust로 작성한 데몬이 WebAssembly 기반 프로바이더(WIT 인터페이스로 명세)를 실행하고, Linux에서는 FUSE, macOS에서는 NFSv4 프론트엔드로 마운트하며, 자격 증명 관리와 바이트 단위 캐싱 계층을 데몬이 중앙에서 책임집니다. 9개 프로바이더의 경로 스키마를 문서화하고 WebAssembly 샌드박싱으로 각 프로바이더의 권한을 명시적으로 선언하는 등, 프로토타입을 넘어선 아키텍처 설계가 눈에 띕니다.

### hieutdo/policyfs

- <https://github.com/hieutdo/policyfs>
- 언어: Go · 스타: 53 · 생성일: 2026-02-26

여러 저장 경로를 하나의 마운트포인트로 통합하고, 패턴 매칭 규칙에 따라 읽기/쓰기를 라우팅하는 Linux FUSE 스토리지 데몬입니다. SQLite로 메타데이터를 인덱싱해 조회만으로는 HDD가 깨어나지 않도록 하는 스핀다운 최적화가 특징이고, 유닛·통합 테스트를 분리해 커버리지를 리포트하는 CI, Debian/Ubuntu·Fedora/EL9용 패키지 저장소, systemd 서비스 템플릿까지 갖추고 있습니다. 지연된 뮤테이션 처리와 인덱싱된 연산에 대한 디버그 로깅 같은 디테일에서, 프로덕션을 염두에 둔 엔지니어링이라는 점이 드러납니다.

### confh/Tiny

- <https://github.com/confh/Tiny>
- 언어: Go · 스타: 57 · 생성일: 2026-05-07

동적 타입 언어를 바이트코드로 컴파일해 자체 가상머신 위에서 실행하는 프로그래밍 언어 프로젝트입니다. 스택 기반 바이트코드 VM에 인터프리터와 JIT 컴파일러를 함께 두고, 뜨거운 바이트코드 경로를 네이티브 WebAssembly로 변환하는 멀티 티어 실행 구조를 취하며, `spawn` 키워드로 OS 레벨 멀티스레딩과 뮤텍스 동기화까지 지원합니다. http·websocket·sqlite·crypto를 포함한 표준 라이브러리와 컴파일러·포매터·LSP·독립 실행파일 패킹까지 갖춘 CLI 도구 체인을 직접 구현했다는 점에서, 기존 런타임을 감싸는 래퍼가 아니라 언어 처리기를 밑바닥부터 쌓아올린 프로젝트임을 알 수 있습니다.

### z1-0/nix-ast

- <https://github.com/z1-0/nix-ast>
- 언어: Haskell · 스타: 55 · 생성일: 2026-06-16

Nix 표현식을 AST로 파싱하고 순수 Nix 함수로 변환·검사·렌더링·평가까지 할 수 있게 해주는 라이브러리로, Haskell의 hnix 파서/평가기를 Import From Derivation(IFD)으로 호출하는 백엔드와 IFD 없이 순수 Nix만으로 동작하는 패턴 매칭·순회 계층을 분리해 설계했습니다. 19종 이상의 AST 노드 타입을 JSON 표현과 함께 문서화하고, 타입 검증된 생성자와 런타임 에러 체크, 배치 처리를 지원하는 CLI까지 갖춰 얇은 편의 레이어가 아니라 실질적인 파서/AST 조작 도구라는 인상을 줍니다. Nix 같은 함수형 설정 언어의 메타프로그래밍을 어떻게 계층화해서 설계하는지 참고하기 좋습니다.

### guimaraeslucas/axonasp

- <https://github.com/guimaraeslucas/axonasp>
- 언어: Go · 스타: 35 · 생성일: 2026-01-25

IIS 없이 Linux·Windows·macOS에서 Classic ASP(VBScript, JavaScript)를 실행할 수 있게 해주는 크로스플랫폼 런타임 엔진입니다. VBScript는 단일 패스 바이트코드 컴파일 후 스택 기반 VM에서, JavaScript는 AST 기반 엔진에서 각각 실행하며, "유휴 상태 18MB(Node.js는 30MB)", "분당 2,000 요청 시 약 100MB 소비" 같은 구체적인 성능 수치를 제시합니다. 독립 HTTP 서버·FastCGI·CLI 실행을 모두 지원하고 60개 이상의 커스텀 라이브러리 함수(JSON, DB, 암호화, 압축)를 직접 구현했다는 점에서, 레거시 언어 런타임을 처음부터 새로 구현하는 작업의 실제 규모를 가늠해볼 수 있습니다.
