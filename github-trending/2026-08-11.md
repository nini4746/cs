---
title: "GitHub Trending 2026-08-11"
tags: [github-trending]
---

오늘 트렌딩 상위권은 "에이전트에게 어떤 산출물을 맡길 것인가"라는 질문이 디자인, 조직, 지식 세 방향으로 갈라져 나타나는 하루입니다. Claude Code가 생성하는 다이어그램의 "AI 티"를 없애려는 스킬(diagram-design)이 가장 큰 폭으로 스타가 늘며 1위에 올랐고, 그 옆에는 230개가 넘는 전문 에이전트 페르소나 모음(agency-agents)과 소프트웨어 개발 생애주기 전체를 슬래시 커맨드로 강제하는 스킬 모음(agent-skills)이 나란히 상위권을 지켰습니다. 여기에 LLM 판단 근거를 지식 그래프로 영구 기록하려는 semantica가 이틀 연속 트렌딩에 오르며 "에이전트가 무엇을 근거로 결정했는가"를 추적하려는 흐름을 보여주고, 신규 릴리스 없이도 nvm이 존재감을 유지하는 것을 보면 화려한 에이전트 도구들 밑에서 여전히 기본기 도구가 자리를 지키고 있다는 것도 알 수 있습니다.

## cathrynlavery/diagram-design

- <https://github.com/cathrynlavery/diagram-design>
- 언어: HTML
- 오늘 스타: 1,612 (총 5,900)

Claude Code가 만드는 다이어그램이 획일적이고 밋밋하다는 문제의식에서 출발한 스킬로, 외부 의존성이나 빌드 단계 없이 그 자체로 완결된 HTML+SVG 형식으로 아키텍처도·플로우차트·타임라인·조직도 등 29가지 다이어그램 타입을 만들어냅니다. 핵심은 "점진적 공개(progressive disclosure)" 구조로, `SKILL.md` 인덱스가 필요한 타입별 참조 파일만 그때그때 불러와 컨텍스트를 절약하고, 웹사이트 URL을 입력하면 60초 만에 그 브랜드의 색상·폰트·대비를 추출해 다이어그램에 그대로 입히는 브랜드 추출 기능도 갖췄습니다. 4의 배수로 맞춘 그리드 시스템과 제한된 팔레트, Playwright 기반 PNG/SVG 내보내기까지 보면, "절차"뿐 아니라 "취향과 미적 기준"을 스킬 파일로 에이전트에 넘기려는 최근 흐름의 또 다른 사례로 볼 수 있습니다.

## msitarzewski/agency-agents

- <https://github.com/msitarzewski/agency-agents>
- 언어: Shell
- 오늘 스타: 971 (총 143,000)

엔지니어링·디자인·마케팅·헬스케어 등 20개 이상 분야에 걸쳐 230개가 넘는 전문 에이전트 페르소나를 모아둔 저장소로, 어제에 이어 오늘도 트렌딩 상위권을 지켰습니다. 각 에이전트가 정체성과 워크플로뿐 아니라 실제 코드 예시와 성공 지표까지 갖췄고, `scripts/convert.sh`와 `scripts/install.sh`로 Claude Code·GitHub Copilot·Cursor·Aider·Windsurf 등 여러 도구의 네이티브 포맷으로 변환·설치할 수 있습니다. 하루 만에 다시 971개의 스타가 붙었다는 것은 "역할별 에이전트 페르소나를 표준 포맷으로 유통한다"는 접근이 일회성 화제가 아니라 스킬 파일 생태계에서 꾸준히 수요가 있는 형태로 자리잡고 있다는 신호로 읽힙니다.

## semantica-agi/semantica

- <https://github.com/semantica-agi/semantica>
- 언어: Python
- 오늘 스타: 884 (총 4,700 이상)

LLM과 에이전트 프레임워크 아래에 놓이는 그래프 네이티브 컨텍스트 인프라로, 임베딩 유사도 대신 지식 그래프에 근거와 출처를 저장하는 것을 목표로 합니다. Ingest→Parse→Extract→Conflict Detection→Dedup→Knowledge Graph→Reasoning/Provenance로 이어지는 파이프라인 위에서 RDF(Oxigraph, Blazegraph, Apache Jena)와 LPG(Neo4j, FalkorDB 등) 저장소를 함께 지원하고, Rete·Datalog·SPARQL 추론 엔진으로 판단 과정을 설명 가능하게 만듭니다. 최신 v0.6.0에서는 Databricks Unity Catalog 커넥터와 SQLite 벡터 스토어 백엔드가 추가되며 규제 산업(금융·의료·정부)을 겨냥한 자체 호스팅 인프라로 발전 중인데, "AI 판단의 근거를 그래프 노드로 영구 기록한다"는 설계가 이틀 연속 트렌딩에 오른 것을 보면 설명 가능성에 대한 수요가 꾸준함을 알 수 있습니다.

## nvm-sh/nvm

- <https://github.com/nvm-sh/nvm>
- 언어: Shell
- 오늘 스타: 18 (총 94,429)

Node.js 여러 버전을 명령줄에서 설치·전환·관리하게 해주는, Node 생태계에서 가장 널리 쓰이는 버전 매니저입니다. POSIX 셸 스크립트로 작성돼 Unix·macOS는 물론 WSL·Git Bash·Cygwin을 통해 Windows에서도 동작하고, `.nvmrc` 자동 인식이나 버전 간 전역 패키지 마이그레이션 같은 실무 편의 기능을 갖췄습니다. 오늘은 새로운 릴리스나 공지 없이 18개의 스타만 늘었는데도 트렌딩 상위권에 들었는데, 화려한 에이전트·AI 도구들이 몰리는 오늘 같은 날에도 개발 환경의 기본기를 다지는 도구가 꾸준히 관심을 받는다는 사실 자체가 눈에 띄는 지점입니다.

## addyosmani/agent-skills

- <https://github.com/addyosmani/agent-skills>
- 언어: JavaScript
- 오늘 스타: 571 (총 86,100)

요구사항 정의부터 배포까지 소프트웨어 개발 생애주기를 Define·Plan·Build·Verify·Review·Ship 6단계, 24개 스킬로 구조화하고 `/spec` `/plan` `/build` `/test` `/review` `/webperf` `/code-simplify` `/ship` 8개 슬래시 커맨드로 각 단계를 강제하는 스킬 모음입니다. 어제에 이어 오늘도 트렌딩에 올랐고, code-reviewer·test-engineer·security-auditor·web-performance-auditor 4개의 전문 페르소나와 `npx skills add`로 70개 이상의 에이전트에 배포 가능한 CLI를 갖췄습니다. Hyrum's Law·Beyonce Rule 같은 《Software Engineering at Google》의 원칙까지 스킬 근거로 명시한 점에서, 시니어 엔지니어의 절차를 프롬프트가 아니라 강제 가능한 게이트로 넘기려는 시도가 계속 힘을 받고 있음을 보여줍니다.

## 오늘의 히든잼

### ayoubnabil/aiondb

- <https://github.com/ayoubnabil/aiondb>
- 언어: Rust
- 스타: 49 (2026-05-13 생성)

PostgreSQL 와이어 프로토콜을 그대로 구사하면서 SQL·그래프·벡터 접근 경로를 하나의 카탈로그 위에 올린, Rust로 밑바닥부터 짠 데이터베이스입니다. 자체 스토리지 엔진 위에 HNSW·IVF-flat 인덱스를 지원하고 `VECTOR(n)`, `HALFVEC(n)` 컬럼과 pgvector 호환 캐스트, L2·코사인·내적·L1 거리 메트릭까지 갖췄으며, 옵티마이저가 논리·물리 플랜을 분리해 처리합니다. Getting Started부터 SQL/Vector Reference, Architecture, Benchmarks까지 문서 체계가 갖춰져 있고 "v0.5, Alpha, 프로덕션 데이터에는 아직 쓰지 말 것"이라고 스스로의 성숙도를 정직하게 밝혀둔 점도 눈에 띕니다. SQL·벡터·그래프 접근을 하나의 카탈로그로 통합하는 설계를 로우레벨에서 들여다보고 싶을 때 참고할 만합니다.

### drmingdrmer/ezraft

- <https://github.com/drmingdrmer/ezraft>
- 언어: Rust
- 스타: 33 (2026-08-01 생성)

검증된 Raft 구현체인 openraft 위에 얹은 "초보자 친화적" 프레임워크로, `EzApp`(상태 연산)과 `EzStorage`(영속성) 두 트레이트만 구현하면 나머지 선출·복제·멤버십·스냅샷·전송 계층은 프레임워크가 알아서 처리한다는 설계입니다. 쓰기는 로그를 통해 복제·커밋된 뒤에만 응답하고, 읽기는 합의 라운드 없이 로컬 메모리에서 바로 답하는 구조로 일관성과 성능을 나눠 다뤘습니다. 완전한 키밸류 스토어 예제와 docs.rs API 문서, 체인지로그를 갖췄지만 "API 자체가 아직 탐색 중이라 안정화 전까지 계속 바뀐다"고 밝힌 점에서 실험 단계임을 숨기지 않습니다. 합의 알고리즘의 복잡도를 최소 API 뒤로 감추는 인터페이스 설계를 공부하기 좋은 자료입니다.

### rustic-compiler/rustc_codegen_c

- <https://github.com/rustic-compiler/rustc_codegen_c>
- 언어: Rust
- 스타: 92 (2026-04-07 생성)

Rust의 MIR(중간 표현)을 LLVM을 거치지 않고 곧바로 C 소스 코드로 변환한 뒤 시스템 C 컴파일러로 오브젝트 파일을 만드는 실험적 컴파일러 백엔드입니다. 이 방식으로 만든 Rust 컴파일러가 rustc의 UI 테스트 스위트(`./x test --stage=2 ui`)를 99.2% 통과했다고 검증 수치를 공개해, 트랜스파일 과정에서 Rust 의미론이 얼마나 보존되는지 구체적으로 밝히고 있습니다. ARM Neon 같은 CPU 특화 명령에 의존하는 일부 크레이트는 실패할 수 있다는 한계도 문서에 명시했습니다. LLVM이 없는 플랫폼에서도 Rust를 돌리기 위해 컴파일러 백엔드 자체를 다른 백엔드로 갈아끼우는 접근과, 그 정합성을 기존 컴파일러의 테스트 스위트로 검증하는 방법론을 함께 볼 수 있습니다.

### LPC4/Full-Stack

- <https://github.com/LPC4/Full-Stack>
- 언어: Rust
- 스타: 87 (2026-04-13 생성)

고급 언어 소스에서 IR, RISC-V 어셈블리, ELF 오브젝트를 거쳐 가상머신 실행까지 전 과정을 브라우저 안에서 실시간으로 보여주는 컴파일러·어셈블러·RISC-V VM·커널 프로젝트입니다. VM은 IF/ID/EX/MEM/WB 인오더 파이프라인에 데이터 포워딩, 로드-유즈 해저드 감지, 2비트 분기 예측, 3단계 캐시 계층, Sv39 MMU까지 갖췄고, M-mode 부트 펌웨어와 프로세스 스케줄링을 갖춘 S-mode 커널 위에서 컴파일러·어셈블러·링커 자신이 사용자 프로세스로 돌아가는 셀프 호스팅까지 달성했습니다. 유닛 테스트, IR·어셈블리 골든 스냅샷 비교, VM 실행 테스트, 커널 통합 테스트로 검증 계층을 나눠뒀고 크레이트마다 설계 결정을 담은 스펙 문서를 갖춘 점도 눈에 띕니다. 컴파일러 이론과 컴퓨터 구조(파이프라이닝, 캐시, 가상 메모리)를 하나의 파이프라인으로 엮어 밑바닥부터 구현한 보기 드문 자료입니다.

### sophie-nguyenthuthuy/data-engineering

- <https://github.com/sophie-nguyenthuthuy/data-engineering>
- 언어: Python
- 스타: 53 (2026-05-12 생성)

스트리밍, CDC, 테이블 포맷, 쿼리 엔진, 합의 알고리즘, 거버넌스까지 데이터 엔지니어링 전 영역을 다루는 159개의 독립 프로젝트를 모은 모노레포입니다. 각 하위 프로젝트가 자체 README·테스트·Docker 이미지·CI 워크플로를 갖췄고, 대부분 Python 표준 라이브러리만으로 Kafka 스타일 append-only 로그, Apache Iceberg 테이블 포맷, LSM 트리, CRDT, MySQL binlog v4 파서, Postgres pgoutput 디코더 같은 것들을 직접 구현합니다. 전체 2,500개가 넘는 테스트와 모든 프로젝트에 대한 `mypy --strict` 통과, 모듈마다 알고리즘·트레이드오프·실패 모드를 설명하는 파일 상단 docstring까지 갖춰 교육적 목적이 뚜렷합니다. 데이터 시스템의 핵심 컴포넌트를 래퍼 라이브러리 없이 하나씩 손으로 짜보고 싶을 때 참고할 만한, 방대하지만 촘촘한 자료입니다.
