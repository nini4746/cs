---
title: "GitHub Trending 2026-08-07"
tags: [github-trending]
---

오늘 트렌딩은 "에이전트에게 무엇을 넘길 것인가"라는 최근 며칠의 흐름이 한 단계 더 진행된 하루입니다. 절차적 지식을 스킬 파일로 정리한 두 프로젝트(addyosmani/agent-skills, mattpocock/skills)와 개발 방법론 전체를 스킬로 구조화한 obra/superpowers가 여전히 상위권을 지키는 가운데, 새로 등장한 PrimeIntellect-ai/prime-agent는 아예 "컨텍스트를 변수로, 도구를 함수 호출로" 다루는 재귀적 언어 모델(RLM) 개념을 내세우며 장기 자율 작업용 에이전트를 표방합니다. 에이전트에게 영속적인 실행 환경을 붙여주는 cloudflare/computer가 이틀 연속 상위권에 남아 있는 것도, 스킬·메모리에 이어 실행 인프라까지 에이전트 생태계가 계층별로 쌓이고 있음을 보여줍니다.

## PrimeIntellect-ai/prime-agent

- <https://github.com/PrimeIntellect-ai/prime-agent>
- 언어: TypeScript
- 오늘 스타: 2,271 (총 5,594)

일반적인 코딩·리서치 작업과 장기 자율 작업을 함께 겨냥한 오픈소스 에이전트로, 오늘 트렌딩에서 가장 큰 폭으로 스타가 늘며 새로 등장했습니다. 컨텍스트를 변수로, 도구를 함수 호출로 다루는 "재귀적 언어 모델(RLM)"과, 보완 프롬프트·메모리·스킬 설명을 세션을 넘어 유지되는 상태로 저장하는 "Continual Harness"라는 두 축으로 구성되어 있습니다. 내장 IPython으로 프로그래밍 가능한 조작을 수행하고, `rlm(...)` 호출로 서브에이전트를 병렬·백그라운드로 띄우며, 터미널 연결이 끊겨도 유지되는 데몬 기반 백그라운드 세션과 자동 압축·영속 목표·하트비트로 장기 작업을 지원하는 구조는, 세션 경계를 넘어서는 에이전트 상태 관리를 어떻게 설계하는지 참고할 만합니다.

## addyosmani/agent-skills

- <https://github.com/addyosmani/agent-skills>
- 언어: JavaScript
- 오늘 스타: 1,131 (총 83,570)

시니어 엔지니어의 워크플로를 Define → Plan → Build → Verify → Review → Ship 6단계로 인코딩한 스킬 모음으로, 어제에 이어 오늘도 상위권을 지켰습니다. 24개 스킬과 8개 슬래시 커맨드, code-reviewer·test-engineer 같은 전문 에이전트 페르소나, 테스트·보안·성능 체크리스트로 구성된 구조는 그대로이며, 이틀 연속 스타가 큰 폭으로 늘고 있는 것을 보면 절차적 지식을 스킬 파일로 표준화하려는 수요가 꾸준함을 보여줍니다.

## cloudflare/computer

- <https://github.com/cloudflare/computer>
- 언어: TypeScript
- 오늘 스타: 894 (총 5,318)

에이전트에게 영속적인 파일시스템과 실행 환경을 붙여주는 모노레포로, 이틀 연속 트렌딩에 올랐습니다. Durable Object가 SQLite로 상태를 관장하고 `workspace.runtime` 단일 진입점 뒤에 컨테이너·아이솔레이트 셸·아이솔레이트 JavaScript 세 가지 실행 백엔드를 감추는 구조는 어제와 동일하며, `@cloudflare/dofs`·`@cloudflare/computer-rpc`·`@cloudflare/computerd` 등으로 나뉜 패키지 구성도 그대로입니다. 오늘 함께 오른 prime-agent의 "세션을 넘어 유지되는 상태" 개념과 나란히 놓고 보면, 에이전트 실행 인프라가 단순 샌드박스를 넘어 영속성을 갖추는 방향으로 수렴하는 흐름을 볼 수 있습니다.

## mattpocock/skills

- <https://github.com/mattpocock/skills>
- 언어: Shell
- 오늘 스타: 2,180 (총 208,325)

"바이브 코딩이 아닌 실전 엔지니어링을 위한" 에이전트 스킬 모음으로, 오늘도 큰 폭의 스타 증가와 함께 상위권을 지켰습니다. 정렬 불일치·장황함·버그·아키텍처 부식이라는 네 가지 실패 모드를 다루는 `grill-with-docs`·`tdd`·코드 리뷰 스킬과, `CONTEXT.md`로 공유 도메인 언어를 유지하는 접근은 어제 소개된 구조와 같습니다. 총 스타가 20만을 넘어선 채로도 하루 2천 개 이상씩 늘고 있다는 점에서, 실무 검증을 앞세운 스킬 모음에 대한 관심이 여전히 뜨겁다는 것을 알 수 있습니다.

## obra/superpowers

- <https://github.com/obra/superpowers>
- 언어: Shell
- 오늘 스타: 794 (총 268,561)

코딩 에이전트를 위한 완결된 소프트웨어 개발 방법론으로, 이전에도 여러 차례 트렌딩에 올랐던 프로젝트가 오늘 다시 상위권에 진입했습니다. 브레인스토밍 → Git worktree 격리 → 계획(2~5분 단위 태스크 분할) → 서브에이전트 기반 개발(2단계 리뷰 포함) → TDD(RED-GREEN-REFACTOR) → 코드 리뷰 → 브랜치 마무리로 이어지는 7단계 워크플로를 13개 이상의 재사용 가능한 스킬로 구성했으며, Claude Code부터 Codex·Cursor·Gemini CLI까지 여러 에이전트 하네시에서 동작하도록 설계되어 있습니다. 오늘 함께 오른 다른 스킬 모음들과 비교해보면, 개별 스킬 나열이 아니라 "임시방편적 접근 대신 체계적인 프로세스"를 전체 개발 라이프사이클 단위로 강제한다는 점이 특징입니다.

## 오늘의 히든잼

### LPC4/Full-Stack

- <https://github.com/LPC4/Full-Stack>
- 언어: Rust
- 스타: 87 (2026-04-13 생성)

컴파일러·어셈블러·링커·가상머신·운영체제를 하나로 묶어 브라우저에서 실시간으로 돌리는 프로젝트입니다. HLL이라는 자체 시스템 언어의 렉서·파서·의미 분석·타입드 SSA IR 생성부터, RISC-V 어셈블리를 재배치 가능한 오브젝트 파일로 바꾸는 어셈블러, 심볼 해석과 재배치로 ELF-64 바이너리를 만드는 링커, 5단계 파이프라인에 MMU·인터럽트 컨트롤러·UART·프레임버퍼까지 갖춘 사이클 단위 RV64IMAFD 프로세서 VM, 그리고 Sv39 페이징과 프로세스 스케줄링을 갖춘 S-mode 커널까지 전체 스택을 자체 구현했습니다. 유닛 테스트, IR·어셈블리 골든 스냅샷 비교, VM에서 HLL 프로그램을 실행해 UART 출력을 검증하는 테스트, OS를 부팅해 게스트 내부에서 프로그램을 실행하고 정상 종료를 확인하는 커널 통합 테스트까지 갖추고 있어, 컴파일러부터 커널까지 전체 파이프라인을 밑바닥부터 구현하며 각 단계를 어떻게 검증하는지 보여주는 사례입니다.

### omiq/rgc-basic

- <https://github.com/omiq/rgc-basic>
- 언어: C
- 스타: 36 (2026-03-09 생성)

C99로 작성된 크로스플랫폼 BASIC 인터프리터로, 커맨드라인용 `basic`, Raylib 기반 40x25 PETSCII 그래픽 환경인 `basic-gfx`, Emscripten으로 빌드한 브라우저/WASM 버전까지 세 가지 실행 환경을 제공합니다. 참조 카운트 기반의 길이-프리픽스 힙 문자열, POKE/PEEK용 가상 메모리 영역, GOSUB 스택과 구조적 제어 흐름을 위한 블록 프레임 스택 같은 실행 모델을 직접 구현했고, VSYNC 동기화 프레임 합성과 스프라이트 배칭을 갖춘 그래픽 파이프라인까지 갖췄습니다. `tests/` 디렉터리의 파일 I/O·자료구조·문자열 함수별 회귀 테스트에 더해, Playwright 헤드리스 크롬으로 터미널·튜토리얼·캔버스 WASM 변형을 자동 검증하는 테스트까지 두 겹의 검증 체계를 갖추고 있어, 클래식 인터프리터를 현대적인 빌드·테스트 파이프라인으로 유지보수하는 방법을 참고하기 좋습니다.

### DDecoene/WebBaseIII

- <https://github.com/DDecoene/WebBaseIII>
- 언어: TypeScript
- 스타: 51 (2026-06-06 생성)

1980년대 dBASE III의 명령어 프롬프트 워크플로를 웹으로 재현한 프로젝트로, `USE customers`·`LIST`·`BROWSE`·`INDEX ON` 같은 명령을 그대로 지원하는 터미널 REPL을 제공합니다. 자체 렉서·파서·실행기를 갖춘 W3Script 인터프리터가 핵심이며, better-sqlite3 위에서 WAL 모드로 데이터를 영속화하고, 웹소켓 연결마다 독립된 인터프리터 세션을 유지하면서도 한 세션의 변경이 다른 세션의 BROWSE 그리드에 실시간으로 반영되는 구조를 갖추고 있습니다. Vitest 단위·통합 테스트와 REPL·필터·인덱싱·프로그램·폼·BROWSE·CSV까지 아우르는 Playwright E2E 테스트를 PR마다 CI로 돌리고 있어, 오래된 도메인 특화 언어의 인터프리터를 현대적인 웹 스택 위에 다시 구현하면서 테스트 커버리지를 어떻게 갖추는지 참고할 만합니다.

### GV4F/ND7.db

- <https://github.com/GV4F/ND7.db>
- 언어: C#
- 스타: 5 (2026-08-05 생성)

Bitcask에서 영감을 받은 로그 구조 저장 방식을 .NET 10 위에 구현한 임베더블 키-값 스토리지 엔진입니다. 디스크에는 순차 append-only로만 기록해 랜덤 시크를 없애고, 메모리에는 키의 오프셋과 크기만 담은 인덱스(`_keyDir`)를 유지해 키당 O(1) 메모리 오버헤드를 노리는 구조이며, 커스텀 바이너리 TCP 프로토콜과 SemaphoreSlim 기반 동시성 제어, 순차 로그 스캔을 통한 크래시 복구까지 갖추고 있습니다. 자체 제작한 벤치마커로 동시 클라이언트 50개 기준 초당 5만 4천 건 이상의 처리량과 P99 지연 40ms 미만을 확인했고, 별도 도구로 최대 30만 건의 동시 PUT 요청까지 부하 테스트한 결과를 공개하고 있어, 만들어진 지 이틀밖에 안 됐지만 Bitcask류 로그 구조 스토리지 엔진의 핵심 트레이드오프(디스크는 순차 쓰기, 메모리는 인덱스만)를 작은 코드베이스로 압축해 보여주는 사례입니다.

### Shivfun99/Pulse-Order

- <https://github.com/Shivfun99/Pulse-Order>
- 언어: C++
- 스타: 118 (2026-06-02 생성)

DPDK로 패킷을 직접 다루는 C++20 기반 저지연 트레이딩 패킷 프로세서로, 시장 데이터 패킷 수신 → 인메모리 Level 2 오더북 갱신 → 임밸런스 기반 전략 실행 → 리스크 검증 → 주문 패킷 송신까지 트레이딩 핫패스 전체를 하나의 run-to-completion 파이프라인으로 구현했습니다. 측정 구간에서는 힙 할당·로깅·문자열 포매팅을 배제했고, 62바이트 고정 바이너리 프레임으로 와이어 프로토콜을 직접 정의했으며, DPDK 링 PMD 가상 경로에서는 이벤트 100만 건 기준 p50 110.844ns/p99 552.217ns를, 커널 기반 AF_PACKET 경로에서는 p50 1.737µs/p99 3.262µs를 실패 없이 기록했다고 밝히고 있습니다. 물리 NIC 지연이나 거래소 왕복 시간, 실제 버스트 상황은 측정 범위에서 명시적으로 제외했다고 밝히는 점까지 포함해서, 저지연 시스템의 핵심 로직을 하드웨어 통합 이전에 어떻게 독립적으로 검증하는지 보여주는 사례입니다.
