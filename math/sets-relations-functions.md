# 집합·관계·함수 (Sets, Relations, Functions)

## 한 줄 요약

집합(모음), 관계(원소 간 연결), 함수(각 입력에 하나의 출력)는 수학의 기본 언어. 카디널리티와 대각선 논법은 무한의 크기를 다루고, 정지 문제 불가능성의 근거가 된다.

## 왜 필요한가

- CS의 기본 어휘 (타입=집합, DB=관계, 함수)
- 대각선 논법이 왜 중요한가 (결정 불가능성)
- 관계의 성질 (동치, 순서)

## 집합 (sets)

원소의 모음 (순서·중복 없음):

- **연산**: 합집합(∪), 교집합(∩), 차집합(−), 여집합, 곱집합(×)
- **부분집합**(⊆), 멱집합(2^S, 모든 부분집합)
- CS: **타입 = 값의 집합** (programming-languages/[[type-systems-advanced]]), 집합 자료구조 (data-structures/[[hash-tables]])
- 곱집합 = 관계형 곱 (database/[[relational-model]]의 CROSS JOIN)

## 관계 (relations)

집합 원소 간의 **연결** (A×B의 부분집합):

- `(a, b) ∈ R`: a와 b가 관계 있음
- **관계형 DB**가 이것 (database/[[relational-model]] - 테이블 = 관계)

관계의 성질:
- **반사(reflexive)**: 모든 a에서 aRa (a = a)
- **대칭(symmetric)**: aRb면 bRa
- **추이(transitive)**: aRb, bRc면 aRc

### 동치 관계 (equivalence)

반사 + 대칭 + 추이 → **동치류(equivalence class)**로 분할:
- 예: mod n 합동 (math/[[modular-arithmetic]]) - 같은 나머지끼리
- automata/[[dfa-minimization]]의 Myhill-Nerode (구별 불가 상태 = 동치류)

### 순서 관계 (order)

반사 + 반대칭 + 추이 → **부분 순서**:
- 위상 정렬(algorithms/[[graph-traversal]])의 의존 순서
- 전체 순서(total order): 모든 쌍 비교 가능 (정렬 algorithms/[[comparison-sorts]])

## 함수 (functions)

각 입력에 **정확히 하나의 출력** (특수한 관계):

`f: A → B` (A=정의역, B=공역):

- **단사(injective, one-to-one)**: 다른 입력 → 다른 출력 (충돌 없음)
- **전사(surjective, onto)**: 모든 출력이 도달됨 (공역 다 씀)
- **전단사(bijective)**: 단사 + 전사 (일대일 대응) → 역함수 존재

CS: 함수형 프로그래밍(programming-languages/[[functional-programming]]), 해시 함수(단사 아님 - 충돌 data-structures/[[hash-tables]]).

## 카디널리티 (cardinality)

집합의 **크기**. 무한 집합도 크기 비교:

- **가산 무한(countable)**: 자연수와 일대일 대응 (정수, 유리수) - "셀 수 있는" 무한
- **비가산 무한(uncountable)**: 자연수보다 큼 (실수) - 대각선 논법으로 증명

## 대각선 논법 (diagonalization)

칸토어의 증명 기법 - **실수가 자연수보다 많음**:

```
실수를 자연수와 대응(나열)했다고 가정
→ 각 실수의 n번째 자리를 바꾼 새 실수 구성 (대각선)
→ 이 새 실수는 목록의 어느 것과도 다름 (n번째 자리가 다름)
→ 목록에 없음 → 나열 불가능 → 비가산
```

**CS의 핵심 응용** - 불가능성 증명:
- **정지 문제 불가능**(automata/[[decidability]]): 대각선으로 자기 참조 모순
- **비가산 언어 vs 가산 프로그램**: 프로그램은 셀 수 있지만(가산) 언어는 비가산 → 대부분 언어는 어떤 프로그램에도 대응 안 됨 (automata/[[decidability]])
- 같은 논법이 러셀 역설, 괴델 불완전성

## CS와의 연결 (종합)

집합·관계·함수가 CS 곳곳:

- **타입 = 집합** → programming-languages/[[type-systems-advanced]]
- **DB = 관계** → database/[[relational-model]]
- **동치류** → automata/[[dfa-minimization]]
- **순서** → 정렬, 위상정렬
- **대각선** → 정지 문제 → automata/[[decidability]]
- **함수** → 함수형 → programming-languages/[[functional-programming]]

## 셀프 체크

> [!question]- 동치 관계와 순서 관계는 각각 어떤 성질의 조합인가? 결과는?
> 동치 관계는 반사 + 대칭 + 추이를 만족하며, 집합을 겹치지 않는 동치류(equivalence class)로 분할한다(예: mod n 합동). 부분 순서 관계는 반사 + 반대칭 + 추이를 만족하며 원소 간 순서를 준다(예: 위상 정렬의 의존 관계). 대칭과 반대칭이 두 관계를 가르는 핵심 차이다.

> [!question]- 단사·전사·전단사 함수의 차이는? 역함수는 언제 존재하나?
> 단사(injective)는 서로 다른 입력이 서로 다른 출력으로 가서 충돌이 없는 것, 전사(surjective)는 공역의 모든 값이 도달되는 것이다. 전단사(bijective)는 단사이면서 전사인 일대일 대응이고, 이때만 역함수가 존재한다. 해시 함수는 단사가 아니어서(충돌 발생) 역함수가 없다.

> [!question]- 가산 무한과 비가산 무한의 차이는? 예를 들면?
> 가산 무한은 자연수와 일대일 대응이 되는 무한으로 "셀 수 있는" 무한이며 정수·유리수가 이에 해당한다. 비가산 무한은 자연수보다 커서 대응이 불가능한 무한으로 실수가 대표적이며, 대각선 논법으로 증명된다. 프로그램은 가산이지만 언어는 비가산이라는 대비가 결정 불가능성의 핵심이다.

> [!question]- 대각선 논법이 CS에서 왜 중요한가?
> 칸토어의 대각선 논법은 어떤 나열도 빠뜨리는 원소를 구성해 "나열 불가능(비가산)"을 증명하는 기법이다. 이 자기 참조 모순 구조가 정지 문제 불가능성 증명에 그대로 쓰이고, 프로그램(가산) 대 언어(비가산)의 크기 차이로 대부분의 언어가 어떤 프로그램에도 대응되지 않음을 보인다. 러셀 역설·괴델 불완전성도 같은 논법이다.

## 연습문제

> [!example]- 증명하라: 정수 집합 ℤ는 가산 무한이다 (자연수와 일대일 대응을 구성).
> **풀이**
> 정수를 0, 1, −1, 2, −2, 3, −3, ... 순으로 지그재그 나열하면 모든 정수가 정확히 한 번씩 등장한다.
> 이 나열은 함수 f: ℕ → ℤ를 준다: f(0)=0, 짝수 위치 2k → k, 홀수 위치 2k−1 → −k.
> 이 f는 단사이자 전사(전단사)이므로 ℤ와 ℕ 사이에 일대일 대응이 존재한다.
> 따라서 ℤ는 가산 무한이다. (무한 집합이 자기 진부분집합인 ℕ과 크기가 같을 수 있음을 보여줌)

> [!example]- 증명하라: 구간 (0,1)의 실수는 비가산이다 (대각선 논법).
> **풀이**
> (0,1)의 실수를 자연수와 대응해 소수 전개로 모두 나열했다고 가정하자: r₁, r₂, r₃, ...
> 새 실수 s를 만든다: s의 n번째 소수 자리를 rₙ의 n번째 자리와 다르게 정한다(예: 그 자리가 5면 4, 아니면 5).
> 그러면 s는 모든 rₙ과 최소 n번째 자리에서 다르므로 목록의 어느 것과도 같지 않다.
> 하지만 s도 (0,1)의 실수이므로 목록에 있어야 한다 - 모순. 따라서 나열은 불가능하고 (0,1)은 비가산이다.

> [!example]- 반례를 찾아라: "대칭이면서 반대칭인 관계는 존재하지 않는다"는 주장이 틀림을 보여라.
> **풀이**
> 반례로 임의 집합 위의 항등 관계(등호) R = {(a,a) : a ∈ A}를 잡는다.
> 대칭 확인: (a,b)∈R이면 a=b이므로 (b,a)=(a,b)∈R. 성립.
> 반대칭 확인: (a,b)∈R이고 (b,a)∈R이면 a=b. 성립(사실 R의 원소는 모두 a=b라 조건이 자명히 만족).
> 따라서 항등 관계는 대칭이면서 동시에 반대칭이므로 주장은 거짓이다. (두 성질이 상호 배타적이지 않음)

## 파인만

> [!note]- 백지에 이 노트 핵심을 남에게 설명하듯 써보라. 막히면 그 부분만 다시.
> **점검 포인트**: 이해했다면 답할 수 있어야 하는 핵심 3가지.
> 1. 반사·대칭·반대칭·추이 성질을 정의하고, 어떤 조합이 동치 관계와 순서 관계를 만드는지 말할 수 있는가?
> 2. 단사·전사·전단사를 구분하고, 언제 역함수가 존재하는지 해시 함수 예로 설명할 수 있는가?
> 3. 대각선 논법의 절차를 재구성하고, 이것이 왜 정지 문제 불가능성의 근거가 되는지(프로그램 가산 vs 언어 비가산) 연결할 수 있는가?

## 연결

- 논리 → [[logic-and-proofs]]
- 관계형 DB → database/[[relational-model]]
- 동치류 (DFA 최소화) → automata/[[dfa-minimization]]
- 대각선 (정지 문제) → automata/[[decidability]]
- 타입 = 집합 → programming-languages/[[type-systems-advanced]]
- mod 동치 → [[modular-arithmetic]]

## 궁금한 것 (나중에)

- [ ] 러셀 역설과 집합론 공리 (ZFC)
- [ ] 연속체 가설
- [ ] 괴델 불완전성과 대각선
- [ ] 순서수·기수 (ordinal/cardinal)

## 출처

- Rosen "Discrete Mathematics" 2장, 칸토어
